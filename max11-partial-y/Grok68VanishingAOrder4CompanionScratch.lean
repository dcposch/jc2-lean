import Grok68VanishingAOrder4CompanionSplitPart25Scratch

/-! # SPEED: drop-in for `Grok68VanishingAOrder4CompanionScratch`.  Every declaration now lives in the
`SplitBase` / `SplitPart*` modules this imports, so every name the original
module exported is still in scope. -/

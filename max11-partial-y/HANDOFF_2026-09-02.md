# Max-11 coordinator handoff — 2026-09-02 (Fable 5 → Fable 5.1 upgrade)

Durable resume point for the max11 + Keystone-1 campaigns. Written before a
clean exit for a model upgrade. Nothing below is lost on restart: all green
work is committed/pushed, in-progress lanes have drafts on disk, and the
box jobs persist independently.

## Committed HEAD

`origin/master` at **`aaa7088`** (max11-partial-y). Keystone-graph at
**`f39fb5c`** era (its Stage-1 sub-theorem committed). Verify with
`git -C ~/code/math/jc2/jc2-lean log --oneline -3`.

## Where Max-11 stands (2 classical citations + these leaves)

- **(4,6)** ✅, **(6,9)** ✅ (registered gcd3-69 chain), **(4,10)** ✅ CLOSED
  and wired into the main library:
  `Max11PlaneKellerGenerationWithFourNormalizedRoutesAfterClosed410` in
  `Max11Assembly.lean` discharges `h410` (full lake build green, 9384 jobs).
- **(6,8)**: scale-zero reduced to ONE Prop
  `IntegratedPolynomialLowerSystem68OpenResidual` (disjunction of 8 endpoint
  packets; non-endpoint region — load wall, mixed-delta face, whole
  3n<2g<5n interior — all CLOSED). scale-two reduced to 2 interfaces
  (aligned-square local-root, nonsquare quadratic packet). vanishing-A
  closure MACHINERY committed (proportionality→zero-remainder→False); only
  the uniform induction step to arbitrary D (m≥2) remains.
- **(6,10)**: linear-root chamber CLOSED (`degreeZeroFace610_linearRoot :
  False`). scale-zero cone tree ~exhausted (residual
  `normalized610ScaleZero_coneResidualClosureResidual`, small). aligned-square
  chamber has its bounded tower set up (base 61, pole 21, ceiling 75).
- **(8,10)**: Pi linear-root tower built through its order-112 ceiling with
  the cancel-expose contradiction (`piFace810_linearRoot_cancelExposePacket`
  + `..._false_of_scaledPi_simplePole`); one glue-lane from the first
  `piFace810_linearRoot : False`. degree-zero Z6 track: clearing power 119,
  pole 1, ceiling 118 committed; numerator base-order computation in flight.

## Five in-flight max11 lanes (relaunch from on-disk drafts)

All were running at exit (6 grok workers). If the local grok processes did
not survive the restart, relaunch each from its draft. Their gate receipts
land in `.max11-lanes/gates/*.receipt`; a green file's SHA-256 appears in a
receipt. Commit each green file with `git add -f <file>` (exact files only,
never bulk-add the scratch tree).

1. `Grok810PiLinearRootFalseScratch.lean` — compose committed Pi pieces into
   `piFace810_linearRoot : False` via the eta=c·j tie (closes an (8,10)
   chamber). HIGH VALUE.
2. `Grok68VanishingAInductionStepScratch.lean` (draft ~732L) — uniform
   companion induction to arbitrary D → `vanishingA_impossible68 : False`.
   HIGH VALUE.
3. `Grok610AlignedSquareTowerOrders61to68Scratch.lean` (draft ~3553L) —
   aligned-square bounded tower orders 61–68 (of 61–75).
4. `Grok610ScaleZeroConeFinalScratch.lean` — close the remaining (6,10)
   scale-zero cones → `normalized610ScaleZero_impossible`.
5. `Grok810DegreeZeroSourceNumeratorScratch.lean` (draft ~3573L) — the
   h^119-cleared Z6 numerator + base order.

## Resume recipe (Grok lanes)

The guarded launcher `scripts/grok_lean_lane.sh` intermittently refuses
(exit 75) even at low worker count — DIRECT-LAUNCH instead:

```sh
cd ~/code/math/jc2/jc2-lean/max11-partial-y
G="$PWD/scripts/no-local-lean"
C="Work autonomously in $PWD. No tracked edits, sorry, new axioms, finite-root shortcuts, or closure overclaims. Do not invoke local Lean/Lake. Use ./scripts/box_lean_verify.sh --file THEFILE for every check. Finish only after literal BUILD_EXIT=0 ERROR_COUNT=0 SORRYAX_COUNT=0. LOCAL_LEAN_GUARD=enabled."
env PATH="$G:$PATH" grok --yolo --output-format plain -p "<per-lane instruction>. $C" >/tmp/lane.log 2>&1 &
```

Detached `&` children do NOT send task-notifications; re-arm a **persistent
Monitor** on `.max11-lanes/gates/*.receipt` count to harvest (see the
2026-09-01 memory notes). Batch bounded-tower orders ~5–8 per lane.

## Verify / box

- Box (AWS box-lean): `ubuntu@54.81.66.156`, key
  `/Users/dc/.ssh/claude-cli.pem`. ~41G free, keep it lean (reap precheck
  dirs). Gate: `./scripts/box_lean_verify.sh --file F.lean` (receipt-only
  first). Regression before/after commits:
  `python3 scripts/test_max11_scratch_cache_keys.py`.
- Palomar submission recipe (proved): ONE self-contained lake package;
  formalization.yaml with current registration facts + qualified priority;
  submit at submit.palomar-registry.org (GitHub sign-in), register from the
  status page. Max-11 will need the sibling projects vendored into one
  package the way `gcd3-69-composition` was.

## Keystone 1 (separate project, parallel, not Grok-402-affected)

`keystone-graph/` — Stage 1 DONE (L10–L15, L17, L18): the ring-theoretic
core K1(3)-(4) — ramification sum law Σe·f=d, empty étale box, valuation
formula e=1+v(𝔇). Remaining: L16 four-box (needs purity import), Stage 2
finite/flat, Stage 3 ZMT-instantiation (Mathlib has it) + Zariski-Nagata
purity (library-scale, dim-2 Auslander-Buchsbaum route). Plan:
`xmodel/k1-mathlib-survey-and-plan-opus5-20260901.md`. K2 queued after K1.

## Next actions on resume

1. Probe grok (`grok --yolo -p "MODEL_OK"`); if 402, ask user for credits.
2. Re-arm the gate-receipt Monitor.
3. Relaunch the 5 lanes above from drafts.
4. Harvest→commit→relaunch until leaves close, then promote each closed
   leaf's chain (see `Grok410ChainPromotionScratch.lean` pattern:
   re-export + audited module list → coordinator wires lakefile roots +
   Max11Assembly import in one commit with full box build).
5. Assemble the unconditional headline, package as one lake package, submit
   + register to Palomar.

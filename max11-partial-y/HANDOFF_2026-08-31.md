# Max-11 coordinator handoff — 2026-08-31 07:00Z

This is the durable handoff for the active goal:

> Prove `gcd3-69-noncube-v2`, then `gcd3-69-noncube`, then Max-11; submit,
> confirm, register, and document each result when ready.

The two `(6,9)` projects are complete and kernel-checked; the source-facing
noncube result is public as `PALOMAR-2026-08-25-000002 v2`.  Local records do
not currently state a separate Palomar id for the v2 cube/noncube composition,
so verify the registry/submission ledger before claiming that id.  Max-11 is
still active and is not yet a theorem or Palomar submission.  The maintainer
has standing approval to confirm and register every submission through
Max-11.

Repository: `/Users/dc/code/math/jc2/jc2-lean`

Working project: `max11-partial-y/`

Latest proof-state commit before this document:
`5a1bacc46199eb1e47c3e737d7d4fa21f64fbe0a`.  The handoff document itself
first landed as `1ef9d56`; use current `origin/master` as the resume HEAD.

## Immediate resumption order

1. Poll the existing 810 Fourteenth AWS job and its five queued consumers.
   Do not restart or duplicate any target while its recorded PID/session is
   live.
2. Resume the 610 chain using the fixed content-stable cache keys.  Start with
   `CompactSource`, then `DifferentialBridge`, post-collapse residual, corrected
   jet, `SourceBridge`, and `CoordinateBridge` in the exact order below.
3. Continue the 68 simple-root descent from the recorded root congruence.
4. Close the three exceptional low-scale leaves, assemble the source-facing
   `(6,9)` theorem and the concrete Max-11 composition, run authoritative
   build/axiom gates, then submit/confirm/register and update `README.md`.

## Safety and verifier rules

- Large Groebner/elimination/CAS jobs go to an isolated `box-lean` AWS machine
  with explicit time/memory bounds and durable `.max11-lanes/cas/` logs.
- `scripts/max11_cas_run.py` owns an isolated process group and is the safe CAS
  wrapper.  Do not restore the old AWK ancestry walk: its associative-array
  lookup inserted empty PIDs and killed unrelated processes.
- Always try `scripts/box_lean_verify.sh --file FILE --receipt-only` first for
  an expensive target.  Run a full leaf gate only on a receipt miss.  Use
  `--reuse-receipt` for automated waiters.
- Commit exact files only.  Never bulk-add the historical scratch tree.
- A requested scratch leaf is intentionally re-elaborated by a full manual
  gate; dependencies hydrate from the content-addressed cache.
- Commit `34bb3bf` fixes cache keys so byte-identical `*Scratch.lean` content
  keeps the same key before and after `git add/commit`.  It excludes the
  recursive scratch closure from the tracked-environment component and can
  promote equivalent legacy artifacts.  Regression command:

  ```sh
  python3 scripts/test_max11_scratch_cache_keys.py
  bash -n scripts/box_lean_verify.sh
  ```

  Both passed before handoff.  For the committed Twelfth source, the stable
  key is `6152787f8f8aaa0c71ff7e0bc6d5572fa9f275c90b54b4a910697d906cbebf8c`;
  the buggy tracked-status key was
  `8467587be36e1567eabbdbcf21af9ef055447a3b596e3915446dbe95601c8d7d`.

## 68 lane: landed result and next algebra

All modules through the pinned simple-root rows are committed and pushed.
The relevant final commits are:

- `9e3f1a5` terminal quotient `2c+3gamma=Bv`, exact `deg v=N`;
- `b36202f` exact `T`, `4H-C0=BT`, `deg T=4N/7`;
- `e2ead2b`, `05dae65` exact `W`/`U` profiles and top cancellation;
- `e0d2daa` proves `C0=81 epsilon gamma+27 i3` is nonzero;
- `eafc5d9` gives `C0 v-C(4 kappa)=Bq`, `deg q=4N/7`;
- `00650d9` proves `IsCoprime B (derivative B)`;
- `2504608` gives `36d^2-C(C0)=Bs`, exact `deg s=13N/7`;
- `5a1bacc` lands the row-zero and row-one root packets.

Exact content now available at every root `x` of `B`:

- `B'(x) != 0`, `d(x) != 0`, and `36 d(x)^2=C0`;
- `C0*(A'(x)v(x)-2B'(x))=162*terminal`;
- `B'(x)*(A(x)W(x)-18gamma)+6A'(x)d(x)
  -9d(x)v(x)v'(x)-9d'(x)v(x)^2=0`.

The next exact calculation was derived but not yet put in a Lean file.  Combine

```text
C0 v' = B' q
72 d d' = B' T
q = -v T                    at B-roots
T = 6 W d                   at B-roots
C0*(A'v-2B') = 162 terminal
36 d^2 = C0
W = 4A+3v^2
```

with pinned row one to obtain, at every root `x` of `B`,

```text
B'(x)d(x) * (v(x)*(W(x)^2-72gamma)+48d(x)) = -108 terminal.
```

With
`L=v*(W^2-C(72gamma))+48d`, squarefreeness should give the polynomial divisor

```text
B | derivative(B)*d*L + C(108*terminal).
```

Build this as the next source-honest module, then use its exact degree/top
profile for the root-count contradiction.

## 610 lane

Landed prerequisites:

- `Fable610ScaleZeroTwelfthDefectScratch.lean`, SHA
  `48dfca7d0a34f7abc66d695089cda48a85fde7de6844e7069d255d58d4484dfe`,
  green in receipt `3b19b0c6dc61a3c78977676de7a17543ac8bd022468290c205908c091225f631`,
  commit `cd8853d`.
- `Sol610ScaleTwoNonzeroFourteenthComplementBackwireScratch.lean`, SHA
  `6471dee4e4d129620e9e090d9dcb01cbb7090aea6803c30deab8e779ef4284bb`,
  same receipt, commit `c63dd60`.
- `Sol610ScaleTwoDegreeZeroLocalPoleScratch.lean`, SHA
  `4a1c1a56401dfe5bd3255eba0ba80a56295b880bcad3e1f8d5ae2c8adbed671d`,
  receipt `dfe4796e2770421def66700234d631398109f26dd59ddcf8fd43a9394617a55a`,
  commit `fdd5b3c`.
- `Sol610ScaleTwoDegreeZeroPrimitiveScratch.lean`, SHA
  `4d297cfa89c3b0c18376c409757ae342bd5e625c265227a75ba1c35258c69628`,
  receipt `01eb6c5eb9016d3ac8d434c32c53975fd3b6b3e6dd55607ce1fb36d3d368bc2b`,
  commit `b4bb0c8`.

Untracked 610 targets, in dependency/verification order:

1. `Sol610ScaleTwoDegreeZeroCompactSourceScratch.lean`
   SHA `d3fd646fbb558a2a6c15ffd0d75e641b00c1484c0367bde904c32d5a69d7d186`.
   Imports landed Primitive.  No successful receipt yet.
2. `Sol610ScaleTwoDegreeZeroDifferentialBridgeScratch.lean`
   SHA `ba1034e15d6921abb13312a627d8ce3b35a9bd677d97cb6eaabc56ffa5b3a081`.
   Imports Primitive; preflight passed; no leaf receipt.
3. `Sol610ScaleTwoNonzeroFourteenthPostCollapseResidualScratch.lean`
   SHA `efaf3c37f5bdc99f98c38084a2b90cfc4fd1485b764e70e97716fe37055050ab`.
   Imports Backwire; drafted only.
4. `Sol610ScaleTwoNonzeroFourteenthPostCollapseHeadSplitScratch.lean`
   SHA `7819ff8b55f8a58d883186879abd797f5f8bb5982ad64f2de6169b61b3d95c18`.
   Imports the prior residual; conditional bilateral split only.
5. `Sol610ScaleTwoDegreeZeroPostCollapseJetScratch.lean`
   SHA `a0bf2b5bca55a68ffdfe421f79b6eaee4c0165bd378a862d1be471174b2f4593`.
   Imports Compact plus post-collapse residual.  This is the corrected
   `h^5*lambda` scaling; earlier jet receipts are obsolete.
6. `Sol610ScaleTwoDegreeZeroPostCollapseSourceBridgeScratch.lean`
   SHA `d880a4939f4b16b8a051dc27c3fe096a0266f0f8c1ebc3f19ea67f6072af9c18`.
   Imports Differential, Jet, and LocalPole.  It currently proves the head
   from an explicitly assumed ordinary-derivative equation, not yet the full
   source wrapper.
7. `Sol610ScaleTwoDegreeZeroPostCollapseCoordinateBridgeScratch.lean`
   SHA `6b0d3ad7c35c036360c0c57dd4fd41161277e59bbc9c6b0198ad3dd927499d2c`.
   Imports SourceBridge; preflight only.  It identifies compact coordinates
   with literal depressed sextic/decic coordinates using Backwire.
8. `Sol610ScaleTwoDegreeZeroPostCollapseHeadSplitScratch.lean`
   SHA `0af0c4e8a2128514a9a3392af80011bb74b192043885a00dff584a572cea6803`.
   Conditional consumer giving `p32=0 or q41=0`; do not claim or gate it as
   closure before source instantiation.

After those gates, add the missing source-facing wrapper that instantiates
`differentialJacobian_affineDepress_sourceToRatFunc68`, uses
`degreeZeroPrimitive610_ratFuncDeriv_eq_simplePole`, applies the coordinate
bridge, and only then feeds the result to the conditional HeadSplit.  Deeper
chambers still remain after that split.  No 610 verifier process was live at
handoff; the redundant Compact/Twelfth run was safely stopped and all its
remote PIDs were verified absent.

## 810 lane and live AWS work

AWS instance:

```text
profile=personal region=us-east-1
instance_id=i-0252f535410c26ebc
name=box-lean type=r6i.4xlarge state=running az=us-east-1a
host=ubuntu@54.81.66.156
cache=/home/ubuntu/jc2-lean/max11-partial-y/.scratch-olean-cache
```

Do not use any unrelated existing instance.  If a genuinely larger CAS job
is needed, the maintainer permits a new `box-lean` instance up to 64 vCPUs.

810 dependency chain and exact state:

1. `Fable810ScaleZeroThirteenthDefectScratch.lean`, SHA
   `121f8cdf7f4c86c6b93d19b137dc0bbf5416d56789ecd2e419d659db8dfe167c`,
   is green: receipt
   `961d08937baf09b3f42f7b351c5fd6a4d6d58eff02cb8d6dd3d5fd1d27508bf6`.
   It is intentionally still untracked: committing it while Fourteenth is
   live would change the running gate's environment audit.
2. `Fable810ScaleZeroFourteenthDefectScratch.lean`, SHA
   `2be60d3c685490fe3aea6826d414b96b4538f512600a6041628a5defe866cf95`,
   is the active exact leaf.
3. `Sol810PiResidualScratch.lean`, SHA
   `ec00bfbbb9b38cedaa0166c42dacaf73a6267f18525a6aeaaa671450d1e77bdc`,
   is queued behind Fourteenth.
4. `Sol810PiDifferentialBridgeScratch.lean`, SHA
   `1ea441ed7ac20dce3ad65f44535aa8527feceaf7660aa179e8227e53db5b33af`,
   and `Sol810PiSourceClearingDraftScratch.lean`, SHA
   `6dc74756306e667423d0f3291b520c17cc25d85aef3af000a7aaba81ef050123`,
   are queued after PiResidual.
5. `Sol810PiLeftUnsolvedUniformBridgeScratch.lean`, SHA
   `20109b4449e9f50dd9f61e8de4855bd2a6c2c05db11be823621b195531bb8306`,
   is queued after SourceClearing and matches its deterministic emitter.
6. `Sol810PiSourcePowerBridgeScratch.lean`, SHA
   `757ea442fd02196a2db0177c7db5aeca433428ddfb7c2eaf7a6414b43bec6517`,
   is queued last.

All pending targets passed preflight/import-collision checks.  Exclude the old
source-invalid `Sol810PiLeftJetBridgeDraftScratch.lean` (SHA
`054581c9428820b0acdecf7ea25de2d1a5eae3ed448dd7482f000643a5ddae9a`)
from the proof chain.

At `2026-08-31T07:00:54Z`, the active Fourteenth details were:

```text
remote workdir=/home/ubuntu/jc2-lean/box-lean-verify.DLdBY1
cache key=d7c7a8c7fe589527ad996851f88b35cea186fd2d8f688a99a588411f01ced83e
timeout=7200s profile=yes
root bash=4015941 flock=4123234 compile bash=4123235
time=4123242 tee=4123243 timeout=4123245 lake=4123247 lean=4123318
lean elapsed=2252s cpu=105% rss=14343976 KiB
live log=.scratch-olean-cache/d7c7a8c7fe589527ad996851f88b35cea186fd2d8f688a99a588411f01ced83e.olean.failure.log.tmp.4123235
```

Local unified sessions and wrapper/SSH/child PIDs:

```text
Fourteenth       session 21853  wrapper 14370  ssh 15559  child 15560
PiResidual       session 32875  wrapper 16698  ssh 19198  child 19200
SourceClearing   session 98789  wrapper 16708  ssh 19197  child 19199
LeftUnsolved     session 89515  wrapper 16699  ssh 19214  child 19216
Differential     session 20309  wrapper 16703  ssh 19196  child 19201
SourcePower      session 19606  wrapper 16718  ssh 19215  child 19217
```

Poll the sessions or run `./scripts/box_lean_status.sh`; do not relaunch on an
observation timeout.  When Fourteenth becomes green, record its exact receipt.
The queued targets should then drain in the listed order.  Repair only a leaf
that actually returns a Lean diagnostic.  Commit the seven 810 Lean targets
only after all running gates have exited and exact receipts have been audited.

The critical mathematical invariant is that the old scalar-solved h97 jet is
not source-honest.  Retain `m1,tau1,g1,k1` and use the unsolved uniform h96
factorization plus the source power/root peel.

## Local scratch custody and clean status

The historical untracked scratch corpus is preserved in place; none of it was
deleted.  The only tracked pre-handoff modification was stashed as
`stash@{0}: handoff-2026-08-31-derive-810-omicron-left`.  It contains the
37-line change to `max11-partial-y/derive_810_omicron_left.py`.

For a clean coordinator view without removing files required by the live AWS
wrappers, local-only ignore patterns were added to the submodule's Git
`info/exclude` for untracked `*Scratch.lean`, Grok scratch notes, and generated
Python scripts.  These patterns do not alter the repository and do not remove
the files.  Use `git status --ignored`, remove the marked handoff block from
`$(git rev-parse --git-path info/exclude)`, or use `git add -f EXACT_FILE` when
landing an audited target.

import Sol68FiveToSixCuspSimultaneousTerminalSplitPart4Scratch

/-! # SPEED: drop-in for `Sol68FiveToSixCuspSimultaneousTerminalScratch`.  Every declaration now lives in the
`SplitBase` / `SplitPart*` modules this imports, so every name the original
module exported is still in scope. -/

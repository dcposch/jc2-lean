import Grok810ScaleZeroQuarticChamberFacesScratch
import Grok810ScaleZeroQuarticChamberColumnsScratch
import Grok810ScaleZeroQuarticChamberRestsScratch
import Grok810ScaleZeroQuarticChamberKillsScratch
import Grok810ScaleZeroQuarticChamberColumns2Scratch
import Grok810ScaleZeroQuarticChamberRests2Scratch
import Grok810ScaleZeroQuarticChamberKills2Scratch
import Grok810ScaleZeroQuarticChamberNuFacesScratch
import Grok810ScaleZeroQuarticChamberColumns3Scratch
import Grok810ScaleZeroQuarticChamberRests3Scratch
import Grok810ScaleZeroQuarticChamberKills3Scratch
import Grok810ScaleZeroQuarticChamberRests4Scratch
import Grok810ScaleZeroQuarticChamberInnerScratch
import Grok810ScaleZeroQuarticChamberKills4Scratch
import Grok810ScaleZeroQuarticChamberColumns5Scratch
import Grok810ScaleZeroQuarticChamberRests5Scratch
import Grok810ScaleZeroQuarticChamberPowerKillsScratch
import Grok810ScaleZeroQuarticChamberRests6Scratch
import Grok810ScaleZeroQuarticChamberInfraN2Scratch
import Grok810ScaleZeroQuarticChamberInfraN3Scratch
import Grok810ScaleZeroQuarticChamberInfraN4Scratch
import Grok810ScaleZeroQuarticChamberInfraN5Scratch
import Grok810ScaleZeroQuarticChamberInfraN6Scratch
import Grok810ScaleZeroQuarticChamberInfraN7Scratch
import Grok810ScaleZeroQuarticChamberInfraOmicronScratch
import Grok810ScaleZeroQuarticChamberInfraPrimitiveScratch
import Grok810ScaleZeroQuarticChamberN5CostLadderScratch
import Grok810ScaleZeroQuarticChamberN7BandsScratch
import Grok810ScaleZeroQuarticChamberUnownedPacketsScratch
import Grok810ScaleZeroQuarticChamberUnownedColumns1Scratch
import Grok810ScaleZeroQuarticChamberUnownedColumns2Scratch
import Grok810ScaleZeroQuarticChamberUnownedColumns3Scratch
import Grok810ScaleZeroQuarticChamberUnownedColumns4Scratch
import Grok810ScaleZeroQuarticChamberUnownedColumns5Scratch
import Grok810ScaleZeroQuarticChamberUnownedRests1Scratch
import Grok810ScaleZeroQuarticChamberUnownedRests2Scratch
import Grok810ScaleZeroQuarticChamberUnownedRests3Scratch
import Grok810ScaleZeroQuarticChamberUnownedRests4Scratch
import Grok810ScaleZeroQuarticChamberUnownedKills1Scratch
import Grok810ScaleZeroQuarticChamberUnownedKills2Scratch
import Grok810ScaleZeroQuarticChamberUnownedKills3Scratch
import Grok810ScaleZeroQuarticChamberUnownedKills4Scratch
import Grok810ScaleZeroQuarticChamberUnownedKills5Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemPackets1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemPackets2Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns2Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns3Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns4Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns5Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns6Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns7Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns8Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns9Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemColumns10Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests2Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests3Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests4Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests5Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests6Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests7Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests8Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests9Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests10Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemRests11Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemBridges1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystemBridges2Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems2Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems3Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems4Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems5Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems6Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems7Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems8Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems9Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems10Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems11Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems12Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems13Scratch
import Grok810ScaleZeroQuarticChamberUnownedSystems14Scratch
import Grok810ScaleZeroQuarticChamberUnownedSplitPacketsScratch
import Grok810ScaleZeroQuarticChamberUnownedSplitColumns1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSplitRests1Scratch
import Grok810ScaleZeroQuarticChamberUnownedSplitsScratch
import Grok810ScaleZeroQuarticChamberUnownedRefinePacketsScratch
import Grok810ScaleZeroQuarticChamberUnownedRefineColumns1Scratch
import Grok810ScaleZeroQuarticChamberUnownedRefineRests1Scratch
import Grok810ScaleZeroQuarticChamberUnownedRefineScratch
import Grok810ScaleZeroQuarticChamberUnownedTreePackets1Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreePackets2Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeColumns1Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeColumns2Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeColumns3Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeColumns4Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeRests1Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeRests2Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeRests3Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreeRests4Scratch
import Grok810ScaleZeroQuarticChamberUnownedTreePiecesScratch

/-! # Plan-tree faces, bridges and scalar certificates

Untracked working note.
-/

open scoped Polynomial.Bivariate

noncomputable section

open Polynomial

namespace Max11DegreeRoutes

set_option linter.unusedVariables false
set_option linter.unusedSimpArgs false
set_option linter.unusedSectionVars false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false
set_option linter.unnecessarySeqFocus false
set_option linter.flexible false
set_option linter.style.haveILetI false
set_option linter.unnecessarySimpa false

set_option maxRecDepth 1000000

variable {k : Type*} [Field k] [CharZero k]

section QuarticChamberUnownedTreeCerts810

/-! ## Face degrees -/

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hFne : F ≠ 0) :
    (xiQuarticChamberFace9500810 A B C D E F G).natDegree =
      2 * F.natDegree := by
  have hc : (5 / 32 : k) ≠ 0 := by norm_num
  simp only [xiQuarticChamberFace9500810]
  rw [natDegree_smul _ hc]
  simp only [natDegree_pow]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem n6QuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0) :
    (n6QuarticChamberFace9500810 A B C D E F G).natDegree =
      A.natDegree + F.natDegree + G.natDegree := by
  have hc : (-(25 / 48 : k)) ≠ 0 := by norm_num
  simp only [n6QuarticChamberFace9500810]
  rw [natDegree_smul _ hc, natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0) :
    (n4QuarticChamberFace9500810 A B C D E F G).natDegree =
      A.natDegree + F.natDegree + G.natDegree := by
  have hc : (-(5 / 8 : k)) ≠ 0 := by norm_num
  simp only [n4QuarticChamberFace9500810]
  rw [natDegree_smul _ hc, natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hDne : D ≠ 0) :
    (muQuarticChamberFace9500810 A B C D E F G).natDegree =
      A.natDegree + 2 * D.natDegree := by
  have hc : (-(5 / 128 : k)) ≠ 0 := by norm_num
  simp only [muQuarticChamberFace9500810]
  rw [natDegree_smul _ hc, natDegree_mul hAne (pow_ne_zero 2 hDne)]
  simp only [natDegree_pow]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n3QuarticChamberFace9500810 A B C D E F G).natDegree =
      A.natDegree + D.natDegree + G.natDegree := by
  have hc : (-(15 / 256 : k)) ≠ 0 := by norm_num
  simp only [n3QuarticChamberFace9500810]
  rw [natDegree_smul _ hc, natDegree_mul (mul_ne_zero hAne hDne) hGne, natDegree_mul hAne hDne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9501810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n4QuarticChamberFace9501810 A B C D E F G).natDegree =
      2 * A.natDegree + D.natDegree + G.natDegree := by
  have hc : (5 / 256 : k) ≠ 0 := by norm_num
  simp only [n4QuarticChamberFace9501810]
  rw [natDegree_smul _ hc, natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
  simp only [natDegree_pow]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem piQuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hGne : G ≠ 0) :
    (piQuarticChamberFace9500810 A B C D E F G).natDegree =
      2 * G.natDegree := by
  have hc : (5 / 4 : k) ≠ 0 := by norm_num
  simp only [piQuarticChamberFace9500810]
  rw [natDegree_smul _ hc]
  simp only [natDegree_pow]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem n2QuarticChamberFace9500810_natDegree
    (A B C D E F G : k[X])
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n2QuarticChamberFace9500810 A B C D E F G).natDegree =
      D.natDegree + G.natDegree := by
  have hc : (5 / 16 : k) ≠ 0 := by norm_num
  simp only [n2QuarticChamberFace9500810]
  rw [natDegree_smul _ hc, natDegree_mul hDne hGne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem kappaQuarticChamberFace9501810_natDegree
    (A B C D E F G : k[X])
    (hDne : D ≠ 0)
    (hEne : E ≠ 0) :
    (kappaQuarticChamberFace9501810 A B C D E F G).natDegree =
      D.natDegree + E.natDegree := by
  have hc : (5 / 16 : k) ≠ 0 := by norm_num
  simp only [kappaQuarticChamberFace9501810]
  rw [natDegree_smul _ hc, natDegree_mul hDne hEne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9502810_natDegree
    (A B C D E F G : k[X])
    (hEne : E ≠ 0)
    (hGne : G ≠ 0) :
    (xiQuarticChamberFace9502810 A B C D E F G).natDegree =
      E.natDegree + G.natDegree := by
  have hc : (5 / 16 : k) ≠ 0 := by norm_num
  simp only [xiQuarticChamberFace9502810]
  rw [natDegree_smul _ hc, natDegree_mul hEne hGne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9504810_natDegree
    (A B C D E F G : k[X])
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hFne : F ≠ 0) :
    (xiQuarticChamberFace9504810 A B C D E F G).natDegree =
      A.natDegree + D.natDegree + F.natDegree := by
  have hc : (-(5 / 64 : k)) ≠ 0 := by norm_num
  simp only [xiQuarticChamberFace9504810]
  rw [natDegree_smul _ hc, natDegree_mul (mul_ne_zero hAne hDne) hFne, natDegree_mul hAne hDne]
  all_goals omega

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9502810_natDegree
    (A B C D E F G : k[X])
    (hEne : E ≠ 0) :
    (muQuarticChamberFace9502810 A B C D E F G).natDegree =
      2 * E.natDegree := by
  have hc : (5 / 32 : k) ≠ 0 := by norm_num
  simp only [muQuarticChamberFace9502810]
  rw [natDegree_smul _ hc]
  simp only [natDegree_pow]
  all_goals omega

/-! ## Top-coefficient bridges -/

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hFne : F ≠ 0) :
    (xiQuarticChamberFace9500810 A B C D E F G).coeff
        (2 * F.natDegree) =
      (5 / 32 : k) * F.leadingCoeff ^ 2 := by
  simp only [xiQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : (F ^ 2).natDegree = 2 * F.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : (F ^ 2).coeff (2 * F.natDegree) = F.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n6QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0) :
    (n6QuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + F.natDegree + G.natDegree) =
      (-(25 / 48 : k)) * A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
  simp only [n6QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * F * G)).natDegree = A.natDegree + F.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
    all_goals omega
  have hc0 : ((A * F * G)).coeff (A.natDegree + F.natDegree + G.natDegree) = A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0) :
    (n4QuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + F.natDegree + G.natDegree) =
      (-(5 / 8 : k)) * A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
  simp only [n4QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * F * G)).natDegree = A.natDegree + F.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
    all_goals omega
  have hc0 : ((A * F * G)).coeff (A.natDegree + F.natDegree + G.natDegree) = A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0) :
    (muQuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + 2 * D.natDegree) =
      (-(5 / 128 : k)) * A.leadingCoeff * D.leadingCoeff ^ 2 := by
  simp only [muQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * D ^ 2)).natDegree = A.natDegree + 2 * D.natDegree := by
    rw [natDegree_mul hAne (pow_ne_zero 2 hDne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * D ^ 2)).coeff (A.natDegree + 2 * D.natDegree) = A.leadingCoeff * D.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n3QuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + D.natDegree + G.natDegree) =
      (-(15 / 256 : k)) * A.leadingCoeff * D.leadingCoeff * G.leadingCoeff := by
  simp only [n3QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * D * G)).natDegree = A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hGne, natDegree_mul hAne hDne]
    all_goals omega
  have hc0 : ((A * D * G)).coeff (A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n4QuarticChamberFace9501810 A B C D E F G).coeff
        (2 * A.natDegree + D.natDegree + G.natDegree) =
      (5 / 256 : k) * A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff := by
  simp only [n4QuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * D * G)).natDegree = 2 * A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * D * G)).coeff (2 * A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem piQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hGne : G ≠ 0) :
    (piQuarticChamberFace9500810 A B C D E F G).coeff
        (2 * G.natDegree) =
      (5 / 4 : k) * G.leadingCoeff ^ 2 := by
  simp only [piQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : (G ^ 2).natDegree = 2 * G.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : (G ^ 2).coeff (2 * G.natDegree) = G.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n2QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hDne : D ≠ 0)
    (hGne : G ≠ 0) :
    (n2QuarticChamberFace9500810 A B C D E F G).coeff
        (D.natDegree + G.natDegree) =
      (5 / 16 : k) * D.leadingCoeff * G.leadingCoeff := by
  simp only [n2QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((D * G)).natDegree = D.natDegree + G.natDegree := by
    rw [natDegree_mul hDne hGne]
    all_goals omega
  have hc0 : ((D * G)).coeff (D.natDegree + G.natDegree) = D.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem kappaQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : B.natDegree + 2 * C.natDegree = A.natDegree + 3 * B.natDegree)
    (ht2 : B.natDegree + G.natDegree = A.natDegree + 3 * B.natDegree)
    (ht3 : C.natDegree + F.natDegree = A.natDegree + 3 * B.natDegree) :
    (kappaQuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + 3 * B.natDegree) =
      (5 / 512 : k) * A.leadingCoeff * B.leadingCoeff ^ 3 + (-(15 / 128 : k)) * B.leadingCoeff * C.leadingCoeff ^ 2 + (5 / 16 : k) * B.leadingCoeff * G.leadingCoeff + (5 / 16 : k) * C.leadingCoeff * F.leadingCoeff := by
  simp only [kappaQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B ^ 3)).natDegree = A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul hAne (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B ^ 3)).coeff (A.natDegree + 3 * B.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 3 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((B * C ^ 2)).natDegree = A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul hBne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((B * C ^ 2)).coeff (A.natDegree + 3 * B.natDegree) = B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((B * G)).natDegree = A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul hBne hGne]
    all_goals omega
  have hc2 : ((B * G)).coeff (A.natDegree + 3 * B.natDegree) = B.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((C * F)).natDegree = A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul hCne hFne]
    all_goals omega
  have hc3 : ((C * F)).coeff (A.natDegree + 3 * B.natDegree) = C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + B.natDegree + F.natDegree = A.natDegree + 2 * B.natDegree + C.natDegree)
    (ht2 : 3 * C.natDegree = A.natDegree + 2 * B.natDegree + C.natDegree)
    (ht3 : C.natDegree + G.natDegree = A.natDegree + 2 * B.natDegree + C.natDegree) :
    (muQuarticChamberFace9501810 A B C D E F G).coeff
        (A.natDegree + 2 * B.natDegree + C.natDegree) =
      (15 / 512 : k) * A.leadingCoeff * B.leadingCoeff ^ 2 * C.leadingCoeff + (-(5 / 64 : k)) * A.leadingCoeff * B.leadingCoeff * F.leadingCoeff + (-(5 / 128 : k)) * C.leadingCoeff ^ 3 + (5 / 16 : k) * C.leadingCoeff * G.leadingCoeff := by
  simp only [muQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B ^ 2 * C)).natDegree = A.natDegree + 2 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hBne)) hCne, natDegree_mul hAne (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B ^ 2 * C)).coeff (A.natDegree + 2 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 2 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B * F)).natDegree = A.natDegree + 2 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) hFne, natDegree_mul hAne hBne]
    all_goals omega
  have hc1 : ((A * B * F)).coeff (A.natDegree + 2 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : (C ^ 3).natDegree = A.natDegree + 2 * B.natDegree + C.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : (C ^ 3).coeff (A.natDegree + 2 * B.natDegree + C.natDegree) = C.leadingCoeff ^ 3 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((C * G)).natDegree = A.natDegree + 2 * B.natDegree + C.natDegree := by
    rw [natDegree_mul hCne hGne]
    all_goals omega
  have hc3 : ((C * G)).coeff (A.natDegree + 2 * B.natDegree + C.natDegree) = C.leadingCoeff * G.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem nuQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + B.natDegree + 2 * C.natDegree = 2 * A.natDegree + 3 * B.natDegree)
    (ht2 : A.natDegree + B.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree)
    (ht3 : A.natDegree + C.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree) :
    (nuQuarticChamberFace9500810 A B C D E F G).coeff
        (2 * A.natDegree + 3 * B.natDegree) =
      (-(5 / 4096 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 + (15 / 1024 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 128 : k)) * A.leadingCoeff * B.leadingCoeff * G.leadingCoeff + (-(5 / 128 : k)) * A.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
  simp only [nuQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B ^ 3)).natDegree = 2 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B ^ 3)).coeff (2 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B * C ^ 2)).natDegree = 2 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) (pow_ne_zero 2 hCne), natDegree_mul hAne hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * B * C ^ 2)).coeff (2 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * B * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) hGne, natDegree_mul hAne hBne]
    all_goals omega
  have hc2 : ((A * B * G)).coeff (2 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * C * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hCne) hFne, natDegree_mul hAne hCne]
    all_goals omega
  have hc3 : ((A * C * F)).coeff (2 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem omicronQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + B.natDegree + 2 * C.natDegree = 3 * A.natDegree + 3 * B.natDegree)
    (ht2 : 2 * A.natDegree + B.natDegree + G.natDegree = 3 * A.natDegree + 3 * B.natDegree)
    (ht3 : 2 * A.natDegree + C.natDegree + F.natDegree = 3 * A.natDegree + 3 * B.natDegree) :
    (omicronQuarticChamberFace9500810 A B C D E F G).coeff
        (3 * A.natDegree + 3 * B.natDegree) =
      (-(5 / 65536 : k)) * A.leadingCoeff ^ 3 * B.leadingCoeff ^ 3 + (15 / 16384 : k) * A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff * G.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff := by
  simp only [omicronQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 3 * B ^ 3)).natDegree = 3 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 3 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 3 * B ^ 3)).coeff (3 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 3 * B.leadingCoeff ^ 3 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * B * C ^ 2)).natDegree = 3 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hBne) (pow_ne_zero 2 hCne), natDegree_mul (pow_ne_zero 2 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * B * C ^ 2)).coeff (3 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 2 * B * G)).natDegree = 3 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hBne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 2 * B * G)).coeff (3 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A ^ 2 * C * F)).natDegree = 3 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A ^ 2 * C * F)).coeff (3 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem piQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + 2 * B.natDegree + 2 * C.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht2 : A.natDegree + 2 * B.natDegree + G.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht3 : A.natDegree + B.natDegree + C.natDegree + F.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht4 : 4 * C.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht5 : A.natDegree + 2 * F.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht6 : 2 * C.natDegree + G.natDegree = 2 * A.natDegree + 4 * B.natDegree)
    (ht7 : 2 * G.natDegree = 2 * A.natDegree + 4 * B.natDegree) :
    (piQuarticChamberFace9501810 A B C D E F G).coeff
        (2 * A.natDegree + 4 * B.natDegree) =
      (15 / 4096 : k) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 4 + (-(45 / 512 : k)) * A.leadingCoeff * B.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 + (5 / 64 : k) * A.leadingCoeff * B.leadingCoeff ^ 2 * G.leadingCoeff + (5 / 32 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * F.leadingCoeff + (15 / 256 : k) * C.leadingCoeff ^ 4 + (-(5 / 16 : k)) * A.leadingCoeff * F.leadingCoeff ^ 2 + (-(5 / 16 : k)) * C.leadingCoeff ^ 2 * G.leadingCoeff + (5 / 4 : k) * G.leadingCoeff ^ 2 := by
  simp only [piQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B ^ 4)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 4 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B ^ 4)).coeff (2 * A.natDegree + 4 * B.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 4 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B ^ 2 * C ^ 2)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hBne)) (pow_ne_zero 2 hCne), natDegree_mul hAne (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * B ^ 2 * C ^ 2)).coeff (2 * A.natDegree + 4 * B.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * B ^ 2 * G)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hBne)) hGne, natDegree_mul hAne (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A * B ^ 2 * G)).coeff (2 * A.natDegree + 4 * B.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 2 * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * B * C * F)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero hAne hBne) hCne) hFne, natDegree_mul (mul_ne_zero hAne hBne) hCne, natDegree_mul hAne hBne]
    all_goals omega
  have hc3 : ((A * B * C * F)).coeff (2 * A.natDegree + 4 * B.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : (C ^ 4).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : (C ^ 4).coeff (2 * A.natDegree + 4 * B.natDegree) = C.leadingCoeff ^ 4 := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd5 : ((A * F ^ 2)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul hAne (pow_ne_zero 2 hFne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc5 : ((A * F ^ 2)).coeff (2 * A.natDegree + 4 * B.natDegree) = A.leadingCoeff * F.leadingCoeff ^ 2 := by
    rw [← hd5, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd6 : ((C ^ 2 * G)).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hGne]
    simp only [natDegree_pow]
    all_goals omega
  have hc6 : ((C ^ 2 * G)).coeff (2 * A.natDegree + 4 * B.natDegree) = C.leadingCoeff ^ 2 * G.leadingCoeff := by
    rw [← hd6, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd7 : (G ^ 2).natDegree = 2 * A.natDegree + 4 * B.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc7 : (G ^ 2).coeff (2 * A.natDegree + 4 * B.natDegree) = G.leadingCoeff ^ 2 := by
    rw [← hd7, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4, hc5, hc6, hc7]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem primitiveQuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 3 * A.natDegree + B.natDegree + 2 * C.natDegree = 4 * A.natDegree + 3 * B.natDegree)
    (ht2 : 3 * A.natDegree + B.natDegree + G.natDegree = 4 * A.natDegree + 3 * B.natDegree)
    (ht3 : 3 * A.natDegree + C.natDegree + F.natDegree = 4 * A.natDegree + 3 * B.natDegree) :
    (primitiveQuarticChamberFace9500810 A B C D E F G).coeff
        (4 * A.natDegree + 3 * B.natDegree) =
      (-(5 / 65536 : k)) * A.leadingCoeff ^ 4 * B.leadingCoeff ^ 3 + (15 / 16384 : k) * A.leadingCoeff ^ 3 * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * B.leadingCoeff * G.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff := by
  simp only [primitiveQuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 4 * B ^ 3)).natDegree = 4 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 4 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 4 * B ^ 3)).coeff (4 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 4 * B.leadingCoeff ^ 3 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 3 * B * C ^ 2)).natDegree = 4 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hBne) (pow_ne_zero 2 hCne), natDegree_mul (pow_ne_zero 3 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 3 * B * C ^ 2)).coeff (4 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 3 * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 3 * B * G)).natDegree = 4 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hBne) hGne, natDegree_mul (pow_ne_zero 3 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 3 * B * G)).coeff (4 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 3 * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A ^ 3 * C * F)).natDegree = 4 * A.natDegree + 3 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 3 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A ^ 3 * C * F)).coeff (4 * A.natDegree + 3 * B.natDegree) = A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * B.natDegree + 2 * C.natDegree = A.natDegree + 4 * B.natDegree)
    (ht2 : 2 * B.natDegree + G.natDegree = A.natDegree + 4 * B.natDegree)
    (ht3 : B.natDegree + C.natDegree + F.natDegree = A.natDegree + 4 * B.natDegree)
    (ht4 : 2 * F.natDegree = A.natDegree + 4 * B.natDegree) :
    (xiQuarticChamberFace9501810 A B C D E F G).coeff
        (A.natDegree + 4 * B.natDegree) =
      (-(15 / 2048 : k)) * A.leadingCoeff * B.leadingCoeff ^ 4 + (45 / 512 : k) * B.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 + (-(5 / 64 : k)) * B.leadingCoeff ^ 2 * G.leadingCoeff + (-(25 / 128 : k)) * B.leadingCoeff * C.leadingCoeff * F.leadingCoeff + (5 / 32 : k) * F.leadingCoeff ^ 2 := by
  simp only [xiQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B ^ 4)).natDegree = A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul hAne (pow_ne_zero 4 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B ^ 4)).coeff (A.natDegree + 4 * B.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 4 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((B ^ 2 * C ^ 2)).natDegree = A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hBne) (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((B ^ 2 * C ^ 2)).coeff (A.natDegree + 4 * B.natDegree) = B.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((B ^ 2 * G)).natDegree = A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hBne) hGne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((B ^ 2 * G)).coeff (A.natDegree + 4 * B.natDegree) = B.leadingCoeff ^ 2 * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((B * C * F)).natDegree = A.natDegree + 4 * B.natDegree := by
    rw [natDegree_mul (mul_ne_zero hBne hCne) hFne, natDegree_mul hBne hCne]
    all_goals omega
  have hc3 : ((B * C * F)).coeff (A.natDegree + 4 * B.natDegree) = B.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : (F ^ 2).natDegree = A.natDegree + 4 * B.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : (F ^ 2).coeff (A.natDegree + 4 * B.natDegree) = F.leadingCoeff ^ 2 := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n2QuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (ht1 : 2 * B.natDegree + F.natDegree = 3 * B.natDegree + C.natDegree) :
    (n2QuarticChamberFace9501810 A B C D E F G).coeff
        (3 * B.natDegree + C.natDegree) =
      (35 / 512 : k) * B.leadingCoeff ^ 3 * C.leadingCoeff + (-(15 / 128 : k)) * B.leadingCoeff ^ 2 * F.leadingCoeff := by
  simp only [n2QuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((B ^ 3 * C)).natDegree = 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (pow_ne_zero 3 hBne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((B ^ 3 * C)).coeff (3 * B.natDegree + C.natDegree) = B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((B ^ 2 * F)).natDegree = 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hBne) hFne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((B ^ 2 * F)).coeff (3 * B.natDegree + C.natDegree) = B.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + 2 * B.natDegree + F.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht2 : B.natDegree + 3 * C.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht3 : B.natDegree + C.natDegree + G.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht4 : 2 * C.natDegree + F.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht5 : F.natDegree + G.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree) :
    (n3QuarticChamberFace9501810 A B C D E F G).coeff
        (A.natDegree + 3 * B.natDegree + C.natDegree) =
      (-(175 / 8192 : k)) * A.leadingCoeff * B.leadingCoeff ^ 3 * C.leadingCoeff + (65 / 2048 : k) * A.leadingCoeff * B.leadingCoeff ^ 2 * F.leadingCoeff + (45 / 1024 : k) * B.leadingCoeff * C.leadingCoeff ^ 3 + (-(15 / 128 : k)) * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff + (-(5 / 64 : k)) * C.leadingCoeff ^ 2 * F.leadingCoeff + (5 / 16 : k) * F.leadingCoeff * G.leadingCoeff := by
  simp only [n3QuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B ^ 3 * C)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 3 hBne)) hCne, natDegree_mul hAne (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B ^ 3 * C)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B ^ 2 * F)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hBne)) hFne, natDegree_mul hAne (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * B ^ 2 * F)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((B * C ^ 3)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul hBne (pow_ne_zero 3 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((B * C ^ 3)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = B.leadingCoeff * C.leadingCoeff ^ 3 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((B * C * G)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hBne hCne) hGne, natDegree_mul hBne hCne]
    all_goals omega
  have hc3 : ((B * C * G)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = B.leadingCoeff * C.leadingCoeff * G.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : ((C ^ 2 * F)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hFne]
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : ((C ^ 2 * F)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = C.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd5 : ((F * G)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul hFne hGne]
    all_goals omega
  have hc5 : ((F * G)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = F.leadingCoeff * G.leadingCoeff := by
    rw [← hd5, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4, hc5]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + 2 * B.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht2 : A.natDegree + B.natDegree + 3 * C.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht3 : A.natDegree + B.natDegree + C.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht4 : A.natDegree + 2 * C.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht5 : A.natDegree + F.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree) :
    (n4QuarticChamberFace9502810 A B C D E F G).coeff
        (2 * A.natDegree + 3 * B.natDegree + C.natDegree) =
      (155 / 8192 : k) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff + (-(55 / 2048 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 2 * F.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 + (5 / 32 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff + (5 / 64 : k) * A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff + (-(5 / 8 : k)) * A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
  simp only [n4QuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B ^ 3 * C)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)) hCne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B ^ 3 * C)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * B ^ 2 * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 2 hBne)) hFne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * B ^ 2 * F)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * B * C ^ 3)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) (pow_ne_zero 3 hCne), natDegree_mul hAne hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A * B * C ^ 3)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * B * C * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero hAne hBne) hCne) hGne, natDegree_mul (mul_ne_zero hAne hBne) hCne, natDegree_mul hAne hBne]
    all_goals omega
  have hc3 : ((A * B * C * G)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : ((A * C ^ 2 * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hCne)) hFne, natDegree_mul hAne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : ((A * C ^ 2 * F)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd5 : ((A * F * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
    all_goals omega
  have hc5 : ((A * F * G)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
    rw [← hd5, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4, hc5]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n5QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + 2 * B.natDegree + F.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht2 : B.natDegree + 3 * C.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht3 : B.natDegree + C.natDegree + G.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht4 : 2 * C.natDegree + F.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht5 : F.natDegree + G.natDegree = A.natDegree + 3 * B.natDegree + C.natDegree) :
    (n5QuarticChamberFace9500810 A B C D E F G).coeff
        (A.natDegree + 3 * B.natDegree + C.natDegree) =
      (-(35 / 4096 : k)) * A.leadingCoeff * B.leadingCoeff ^ 3 * C.leadingCoeff + (5 / 512 : k) * A.leadingCoeff * B.leadingCoeff ^ 2 * F.leadingCoeff + (45 / 1024 : k) * B.leadingCoeff * C.leadingCoeff ^ 3 + (-(15 / 128 : k)) * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff + (-(5 / 64 : k)) * C.leadingCoeff ^ 2 * F.leadingCoeff + (5 / 16 : k) * F.leadingCoeff * G.leadingCoeff := by
  simp only [n5QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B ^ 3 * C)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 3 hBne)) hCne, natDegree_mul hAne (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B ^ 3 * C)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B ^ 2 * F)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hBne)) hFne, natDegree_mul hAne (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * B ^ 2 * F)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((B * C ^ 3)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul hBne (pow_ne_zero 3 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((B * C ^ 3)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = B.leadingCoeff * C.leadingCoeff ^ 3 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((B * C * G)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hBne hCne) hGne, natDegree_mul hBne hCne]
    all_goals omega
  have hc3 : ((B * C * G)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = B.leadingCoeff * C.leadingCoeff * G.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : ((C ^ 2 * F)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hFne]
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : ((C ^ 2 * F)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = C.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd5 : ((F * G)).natDegree = A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul hFne hGne]
    all_goals omega
  have hc5 : ((F * G)).coeff (A.natDegree + 3 * B.natDegree + C.natDegree) = F.leadingCoeff * G.leadingCoeff := by
    rw [← hd5, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4, hc5]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n6QuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + 2 * B.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht2 : A.natDegree + B.natDegree + 3 * C.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht3 : A.natDegree + B.natDegree + C.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht4 : A.natDegree + 2 * C.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht5 : A.natDegree + F.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree) :
    (n6QuarticChamberFace9501810 A B C D E F G).coeff
        (2 * A.natDegree + 3 * B.natDegree + C.natDegree) =
      (145 / 12288 : k) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff + (-(25 / 1536 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 2 * F.leadingCoeff + (-(45 / 1024 : k)) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 + (15 / 128 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff + (5 / 96 : k) * A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff + (-(25 / 48 : k)) * A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
  simp only [n6QuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B ^ 3 * C)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)) hCne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B ^ 3 * C)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * B ^ 2 * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 2 hBne)) hFne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 2 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * B ^ 2 * F)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * B * C ^ 3)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) (pow_ne_zero 3 hCne), natDegree_mul hAne hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A * B * C ^ 3)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * B * C * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero hAne hBne) hCne) hGne, natDegree_mul (mul_ne_zero hAne hBne) hCne, natDegree_mul hAne hBne]
    all_goals omega
  have hc3 : ((A * B * C * G)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd4 : ((A * C ^ 2 * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hCne)) hFne, natDegree_mul hAne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc4 : ((A * C ^ 2 * F)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd4, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd5 : ((A * F * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hFne) hGne, natDegree_mul hAne hFne]
    all_goals omega
  have hc5 : ((A * F * G)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * F.leadingCoeff * G.leadingCoeff := by
    rw [← hd5, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3, hc4, hc5]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n7QuarticChamberFace9500810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + B.natDegree + 3 * C.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht2 : A.natDegree + B.natDegree + C.natDegree + G.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree)
    (ht3 : A.natDegree + 2 * C.natDegree + F.natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree) :
    (n7QuarticChamberFace9500810 A B C D E F G).coeff
        (2 * A.natDegree + 3 * B.natDegree + C.natDegree) =
      (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff + (15 / 512 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 + (-(5 / 64 : k)) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff + (-(5 / 64 : k)) * A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff := by
  simp only [n7QuarticChamberFace9500810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B ^ 3 * C)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)) hCne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 3 hBne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B ^ 3 * C)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff ^ 3 * C.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B * C ^ 3)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) (pow_ne_zero 3 hCne), natDegree_mul hAne hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * B * C ^ 3)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 3 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * B * C * G)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero hAne hBne) hCne) hGne, natDegree_mul (mul_ne_zero hAne hBne) hCne, natDegree_mul hAne hBne]
    all_goals omega
  have hc2 : ((A * B * C * G)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * C ^ 2 * F)).natDegree = 2 * A.natDegree + 3 * B.natDegree + C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hCne)) hFne, natDegree_mul hAne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A * C ^ 2 * F)).coeff (2 * A.natDegree + 3 * B.natDegree + C.natDegree) = A.leadingCoeff * C.leadingCoeff ^ 2 * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem kappaQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hDne : D ≠ 0)
    (hEne : E ≠ 0) :
    (kappaQuarticChamberFace9501810 A B C D E F G).coeff
        (D.natDegree + E.natDegree) =
      (5 / 16 : k) * D.leadingCoeff * E.leadingCoeff := by
  simp only [kappaQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((D * E)).natDegree = D.natDegree + E.natDegree := by
    rw [natDegree_mul hDne hEne]
    all_goals omega
  have hc0 : ((D * E)).coeff (D.natDegree + E.natDegree) = D.leadingCoeff * E.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hEne : E ≠ 0)
    (hGne : G ≠ 0) :
    (xiQuarticChamberFace9502810 A B C D E F G).coeff
        (E.natDegree + G.natDegree) =
      (5 / 16 : k) * E.leadingCoeff * G.leadingCoeff := by
  simp only [xiQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((E * G)).natDegree = E.natDegree + G.natDegree := by
    rw [natDegree_mul hEne hGne]
    all_goals omega
  have hc0 : ((E * G)).coeff (E.natDegree + G.natDegree) = E.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + E.natDegree + F.natDegree = A.natDegree + D.natDegree + G.natDegree) :
    (n3QuarticChamberFace9502810 A B C D E F G).coeff
        (A.natDegree + D.natDegree + G.natDegree) =
      (-(15 / 256 : k)) * A.leadingCoeff * D.leadingCoeff * G.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
  simp only [n3QuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * D * G)).natDegree = A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hGne, natDegree_mul hAne hDne]
    all_goals omega
  have hc0 : ((A * D * G)).coeff (A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * E * F)).natDegree = A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hEne) hFne, natDegree_mul hAne hEne]
    all_goals omega
  have hc1 : ((A * E * F)).coeff (A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + E.natDegree + F.natDegree = 2 * A.natDegree + D.natDegree + G.natDegree) :
    (n4QuarticChamberFace9503810 A B C D E F G).coeff
        (2 * A.natDegree + D.natDegree + G.natDegree) =
      (5 / 256 : k) * A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff + (5 / 256 : k) * A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
  simp only [n4QuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * D * G)).natDegree = 2 * A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * D * G)).coeff (2 * A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * E * F)).natDegree = 2 * A.natDegree + D.natDegree + G.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hEne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hEne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * E * F)).coeff (2 * A.natDegree + D.natDegree + G.natDegree) = A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem piQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * G.natDegree = A.natDegree + 2 * F.natDegree) :
    (piQuarticChamberFace9502810 A B C D E F G).coeff
        (A.natDegree + 2 * F.natDegree) =
      (-(5 / 16 : k)) * A.leadingCoeff * F.leadingCoeff ^ 2 + (5 / 4 : k) * G.leadingCoeff ^ 2 := by
  simp only [piQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * F ^ 2)).natDegree = A.natDegree + 2 * F.natDegree := by
    rw [natDegree_mul hAne (pow_ne_zero 2 hFne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * F ^ 2)).coeff (A.natDegree + 2 * F.natDegree) = A.leadingCoeff * F.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : (G ^ 2).natDegree = A.natDegree + 2 * F.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : (G ^ 2).coeff (A.natDegree + 2 * F.natDegree) = G.leadingCoeff ^ 2 := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : E.natDegree + G.natDegree = A.natDegree + D.natDegree + F.natDegree) :
    (xiQuarticChamberFace9503810 A B C D E F G).coeff
        (A.natDegree + D.natDegree + F.natDegree) =
      (-(5 / 64 : k)) * A.leadingCoeff * D.leadingCoeff * F.leadingCoeff + (5 / 16 : k) * E.leadingCoeff * G.leadingCoeff := by
  simp only [xiQuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * D * F)).natDegree = A.natDegree + D.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hFne, natDegree_mul hAne hDne]
    all_goals omega
  have hc0 : ((A * D * F)).coeff (A.natDegree + D.natDegree + F.natDegree) = A.leadingCoeff * D.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((E * G)).natDegree = A.natDegree + D.natDegree + F.natDegree := by
    rw [natDegree_mul hEne hGne]
    all_goals omega
  have hc1 : ((E * G)).coeff (A.natDegree + D.natDegree + F.natDegree) = E.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9504810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hDne : D ≠ 0)
    (hFne : F ≠ 0) :
    (xiQuarticChamberFace9504810 A B C D E F G).coeff
        (A.natDegree + D.natDegree + F.natDegree) =
      (-(5 / 64 : k)) * A.leadingCoeff * D.leadingCoeff * F.leadingCoeff := by
  simp only [xiQuarticChamberFace9504810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * D * F)).natDegree = A.natDegree + D.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hFne, natDegree_mul hAne hDne]
    all_goals omega
  have hc0 : ((A * D * F)).coeff (A.natDegree + D.natDegree + F.natDegree) = A.leadingCoeff * D.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hEne : E ≠ 0) :
    (muQuarticChamberFace9502810 A B C D E F G).coeff
        (2 * E.natDegree) =
      (5 / 32 : k) * E.leadingCoeff ^ 2 := by
  simp only [muQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : (E ^ 2).natDegree = 2 * E.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : (E ^ 2).coeff (2 * E.natDegree) = E.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem kappaQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : B.natDegree + G.natDegree = B.natDegree + 2 * C.natDegree)
    (ht2 : C.natDegree + F.natDegree = B.natDegree + 2 * C.natDegree)
    (ht3 : D.natDegree + E.natDegree = B.natDegree + 2 * C.natDegree) :
    (kappaQuarticChamberFace9502810 A B C D E F G).coeff
        (B.natDegree + 2 * C.natDegree) =
      (-(15 / 128 : k)) * B.leadingCoeff * C.leadingCoeff ^ 2 + (5 / 16 : k) * B.leadingCoeff * G.leadingCoeff + (5 / 16 : k) * C.leadingCoeff * F.leadingCoeff + (5 / 16 : k) * D.leadingCoeff * E.leadingCoeff := by
  simp only [kappaQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((B * C ^ 2)).natDegree = B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul hBne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((B * C ^ 2)).coeff (B.natDegree + 2 * C.natDegree) = B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((B * G)).natDegree = B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul hBne hGne]
    all_goals omega
  have hc1 : ((B * G)).coeff (B.natDegree + 2 * C.natDegree) = B.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((C * F)).natDegree = B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul hCne hFne]
    all_goals omega
  have hc2 : ((C * F)).coeff (B.natDegree + 2 * C.natDegree) = C.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((D * E)).natDegree = B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul hDne hEne]
    all_goals omega
  have hc3 : ((D * E)).coeff (B.natDegree + 2 * C.natDegree) = D.leadingCoeff * E.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem muQuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hCne : C ≠ 0)
    (hEne : E ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : C.natDegree + G.natDegree = 3 * C.natDegree)
    (ht2 : 2 * E.natDegree = 3 * C.natDegree) :
    (muQuarticChamberFace9503810 A B C D E F G).coeff
        (3 * C.natDegree) =
      (-(5 / 128 : k)) * C.leadingCoeff ^ 3 + (5 / 16 : k) * C.leadingCoeff * G.leadingCoeff + (5 / 32 : k) * E.leadingCoeff ^ 2 := by
  simp only [muQuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : (C ^ 3).natDegree = 3 * C.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : (C ^ 3).coeff (3 * C.natDegree) = C.leadingCoeff ^ 3 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((C * G)).natDegree = 3 * C.natDegree := by
    rw [natDegree_mul hCne hGne]
    all_goals omega
  have hc1 : ((C * G)).coeff (3 * C.natDegree) = C.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : (E ^ 2).natDegree = 3 * C.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : (E ^ 2).coeff (3 * C.natDegree) = E.leadingCoeff ^ 2 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n2QuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * C.natDegree + D.natDegree = B.natDegree + C.natDegree + E.natDegree)
    (ht2 : D.natDegree + G.natDegree = B.natDegree + C.natDegree + E.natDegree)
    (ht3 : E.natDegree + F.natDegree = B.natDegree + C.natDegree + E.natDegree) :
    (n2QuarticChamberFace9502810 A B C D E F G).coeff
        (B.natDegree + C.natDegree + E.natDegree) =
      (-(15 / 64 : k)) * B.leadingCoeff * C.leadingCoeff * E.leadingCoeff + (-(15 / 128 : k)) * C.leadingCoeff ^ 2 * D.leadingCoeff + (5 / 16 : k) * D.leadingCoeff * G.leadingCoeff + (5 / 16 : k) * E.leadingCoeff * F.leadingCoeff := by
  simp only [n2QuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((B * C * E)).natDegree = B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero hBne hCne) hEne, natDegree_mul hBne hCne]
    all_goals omega
  have hc0 : ((B * C * E)).coeff (B.natDegree + C.natDegree + E.natDegree) = B.leadingCoeff * C.leadingCoeff * E.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((C ^ 2 * D)).natDegree = B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((C ^ 2 * D)).coeff (B.natDegree + C.natDegree + E.natDegree) = C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((D * G)).natDegree = B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul hDne hGne]
    all_goals omega
  have hc2 : ((D * G)).coeff (B.natDegree + C.natDegree + E.natDegree) = D.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((E * F)).natDegree = B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul hEne hFne]
    all_goals omega
  have hc3 : ((E * F)).coeff (B.natDegree + C.natDegree + E.natDegree) = E.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + 2 * C.natDegree + D.natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree)
    (ht2 : A.natDegree + D.natDegree + G.natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree)
    (ht3 : A.natDegree + E.natDegree + F.natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree) :
    (n3QuarticChamberFace9503810 A B C D E F G).coeff
        (A.natDegree + B.natDegree + C.natDegree + E.natDegree) =
      (45 / 1024 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * E.leadingCoeff + (45 / 2048 : k) * A.leadingCoeff * C.leadingCoeff ^ 2 * D.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * D.leadingCoeff * G.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
  simp only [n3QuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B * C * E)).natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero hAne hBne) hCne) hEne, natDegree_mul (mul_ne_zero hAne hBne) hCne, natDegree_mul hAne hBne]
    all_goals omega
  have hc0 : ((A * B * C * E)).coeff (A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff * E.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * C ^ 2 * D)).natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hCne)) hDne, natDegree_mul hAne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A * C ^ 2 * D)).coeff (A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff * C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * D * G)).natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hGne, natDegree_mul hAne hDne]
    all_goals omega
  have hc2 : ((A * D * G)).coeff (A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * E * F)).natDegree = A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hEne) hFne, natDegree_mul hAne hEne]
    all_goals omega
  have hc3 : ((A * E * F)).coeff (A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9504810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + 2 * C.natDegree + D.natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree)
    (ht2 : 2 * A.natDegree + D.natDegree + G.natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree)
    (ht3 : 2 * A.natDegree + E.natDegree + F.natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) :
    (n4QuarticChamberFace9504810 A B C D E F G).coeff
        (2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) =
      (-(15 / 1024 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff * E.leadingCoeff + (-(15 / 2048 : k)) * A.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 * D.leadingCoeff + (5 / 256 : k) * A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff + (5 / 256 : k) * A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
  simp only [n4QuarticChamberFace9504810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B * C * E)).natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero (mul_ne_zero (pow_ne_zero 2 hAne) hBne) hCne) hEne, natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hBne) hCne, natDegree_mul (pow_ne_zero 2 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B * C * E)).coeff (2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff * E.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * C ^ 2 * D)).natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 2 hCne)) hDne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * C ^ 2 * D)).coeff (2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 2 * D * G)).natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 2 * D * G)).coeff (2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A ^ 2 * E * F)).natDegree = 2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hEne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hEne]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A ^ 2 * E * F)).coeff (2 * A.natDegree + B.natDegree + C.natDegree + E.natDegree) = A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem nuQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + B.natDegree + G.natDegree = A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht2 : A.natDegree + C.natDegree + F.natDegree = A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht3 : A.natDegree + D.natDegree + E.natDegree = A.natDegree + B.natDegree + 2 * C.natDegree) :
    (nuQuarticChamberFace9501810 A B C D E F G).coeff
        (A.natDegree + B.natDegree + 2 * C.natDegree) =
      (15 / 1024 : k) * A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 128 : k)) * A.leadingCoeff * B.leadingCoeff * G.leadingCoeff + (-(5 / 128 : k)) * A.leadingCoeff * C.leadingCoeff * F.leadingCoeff + (-(5 / 128 : k)) * A.leadingCoeff * D.leadingCoeff * E.leadingCoeff := by
  simp only [nuQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * B * C ^ 2)).natDegree = A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) (pow_ne_zero 2 hCne), natDegree_mul hAne hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * B * C ^ 2)).coeff (A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * B * G)).natDegree = A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hBne) hGne, natDegree_mul hAne hBne]
    all_goals omega
  have hc1 : ((A * B * G)).coeff (A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * C * F)).natDegree = A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hCne) hFne, natDegree_mul hAne hCne]
    all_goals omega
  have hc2 : ((A * C * F)).coeff (A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A * D * E)).natDegree = A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hEne, natDegree_mul hAne hDne]
    all_goals omega
  have hc3 : ((A * D * E)).coeff (A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem omicronQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + B.natDegree + G.natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht2 : 2 * A.natDegree + C.natDegree + F.natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht3 : 2 * A.natDegree + D.natDegree + E.natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree) :
    (omicronQuarticChamberFace9501810 A B C D E F G).coeff
        (2 * A.natDegree + B.natDegree + 2 * C.natDegree) =
      (15 / 16384 : k) * A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * B.leadingCoeff * G.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * D.leadingCoeff * E.leadingCoeff := by
  simp only [omicronQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * B * C ^ 2)).natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hBne) (pow_ne_zero 2 hCne), natDegree_mul (pow_ne_zero 2 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * B * C ^ 2)).coeff (2 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * B * G)).natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hBne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * B * G)).coeff (2 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 2 * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 2 * C * F)).natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 2 * C * F)).coeff (2 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A ^ 2 * D * E)).natDegree = 2 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hEne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A ^ 2 * D * E)).coeff (2 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem piQuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hCne : C ≠ 0)
    (hEne : E ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * C.natDegree + G.natDegree = 4 * C.natDegree)
    (ht2 : C.natDegree + 2 * E.natDegree = 4 * C.natDegree)
    (ht3 : 2 * G.natDegree = 4 * C.natDegree) :
    (piQuarticChamberFace9503810 A B C D E F G).coeff
        (4 * C.natDegree) =
      (15 / 256 : k) * C.leadingCoeff ^ 4 + (-(5 / 16 : k)) * C.leadingCoeff ^ 2 * G.leadingCoeff + (-(5 / 8 : k)) * C.leadingCoeff * E.leadingCoeff ^ 2 + (5 / 4 : k) * G.leadingCoeff ^ 2 := by
  simp only [piQuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : (C ^ 4).natDegree = 4 * C.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : (C ^ 4).coeff (4 * C.natDegree) = C.leadingCoeff ^ 4 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((C ^ 2 * G)).natDegree = 4 * C.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hGne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((C ^ 2 * G)).coeff (4 * C.natDegree) = C.leadingCoeff ^ 2 * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((C * E ^ 2)).natDegree = 4 * C.natDegree := by
    rw [natDegree_mul hCne (pow_ne_zero 2 hEne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((C * E ^ 2)).coeff (4 * C.natDegree) = C.leadingCoeff * E.leadingCoeff ^ 2 := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : (G ^ 2).natDegree = 4 * C.natDegree := by
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : (G ^ 2).coeff (4 * C.natDegree) = G.leadingCoeff ^ 2 := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem primitiveQuarticChamberFace9501810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hBne : B ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 3 * A.natDegree + B.natDegree + G.natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht2 : 3 * A.natDegree + C.natDegree + F.natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree)
    (ht3 : 3 * A.natDegree + D.natDegree + E.natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree) :
    (primitiveQuarticChamberFace9501810 A B C D E F G).coeff
        (3 * A.natDegree + B.natDegree + 2 * C.natDegree) =
      (15 / 16384 : k) * A.leadingCoeff ^ 3 * B.leadingCoeff * C.leadingCoeff ^ 2 + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * B.leadingCoeff * G.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * D.leadingCoeff * E.leadingCoeff := by
  simp only [primitiveQuarticChamberFace9501810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 3 * B * C ^ 2)).natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hBne) (pow_ne_zero 2 hCne), natDegree_mul (pow_ne_zero 3 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 3 * B * C ^ 2)).coeff (3 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 3 * B.leadingCoeff * C.leadingCoeff ^ 2 := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 3 * B * G)).natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hBne) hGne, natDegree_mul (pow_ne_zero 3 hAne) hBne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 3 * B * G)).coeff (3 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 3 * B.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 3 * C * F)).natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 3 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 3 * C * F)).coeff (3 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd3 : ((A ^ 3 * D * E)).natDegree = 3 * A.natDegree + B.natDegree + 2 * C.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hDne) hEne, natDegree_mul (pow_ne_zero 3 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc3 : ((A ^ 3 * D * E)).coeff (3 * A.natDegree + B.natDegree + 2 * C.natDegree) = A.leadingCoeff ^ 3 * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd3, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2, hc3]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem xiQuarticChamberFace9505810_coeff_top
    {A B C D E F G : k[X]}
    (hCne : C ≠ 0)
    (hEne : E ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : E.natDegree + G.natDegree = 2 * C.natDegree + E.natDegree) :
    (xiQuarticChamberFace9505810 A B C D E F G).coeff
        (2 * C.natDegree + E.natDegree) =
      (-(15 / 128 : k)) * C.leadingCoeff ^ 2 * E.leadingCoeff + (5 / 16 : k) * E.leadingCoeff * G.leadingCoeff := by
  simp only [xiQuarticChamberFace9505810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((C ^ 2 * E)).natDegree = 2 * C.natDegree + E.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hEne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((C ^ 2 * E)).coeff (2 * C.natDegree + E.natDegree) = C.leadingCoeff ^ 2 * E.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((E * G)).natDegree = 2 * C.natDegree + E.natDegree := by
    rw [natDegree_mul hEne hGne]
    all_goals omega
  have hc1 : ((E * G)).coeff (2 * C.natDegree + E.natDegree) = E.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem kappaQuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (ht1 : D.natDegree + E.natDegree = C.natDegree + F.natDegree) :
    (kappaQuarticChamberFace9503810 A B C D E F G).coeff
        (C.natDegree + F.natDegree) =
      (5 / 16 : k) * C.leadingCoeff * F.leadingCoeff + (5 / 16 : k) * D.leadingCoeff * E.leadingCoeff := by
  simp only [kappaQuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((C * F)).natDegree = C.natDegree + F.natDegree := by
    rw [natDegree_mul hCne hFne]
    all_goals omega
  have hc0 : ((C * F)).coeff (C.natDegree + F.natDegree) = C.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((D * E)).natDegree = C.natDegree + F.natDegree := by
    rw [natDegree_mul hDne hEne]
    all_goals omega
  have hc1 : ((D * E)).coeff (C.natDegree + F.natDegree) = D.leadingCoeff * E.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n2QuarticChamberFace9503810_coeff_top
    {A B C D E F G : k[X]}
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : D.natDegree + G.natDegree = 2 * C.natDegree + D.natDegree)
    (ht2 : E.natDegree + F.natDegree = 2 * C.natDegree + D.natDegree) :
    (n2QuarticChamberFace9503810 A B C D E F G).coeff
        (2 * C.natDegree + D.natDegree) =
      (-(15 / 128 : k)) * C.leadingCoeff ^ 2 * D.leadingCoeff + (5 / 16 : k) * D.leadingCoeff * G.leadingCoeff + (5 / 16 : k) * E.leadingCoeff * F.leadingCoeff := by
  simp only [n2QuarticChamberFace9503810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((C ^ 2 * D)).natDegree = 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (pow_ne_zero 2 hCne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((C ^ 2 * D)).coeff (2 * C.natDegree + D.natDegree) = C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((D * G)).natDegree = 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul hDne hGne]
    all_goals omega
  have hc1 : ((D * G)).coeff (2 * C.natDegree + D.natDegree) = D.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((E * F)).natDegree = 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul hEne hFne]
    all_goals omega
  have hc2 : ((E * F)).coeff (2 * C.natDegree + D.natDegree) = E.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n3QuarticChamberFace9504810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : A.natDegree + D.natDegree + G.natDegree = A.natDegree + 2 * C.natDegree + D.natDegree)
    (ht2 : A.natDegree + E.natDegree + F.natDegree = A.natDegree + 2 * C.natDegree + D.natDegree) :
    (n3QuarticChamberFace9504810 A B C D E F G).coeff
        (A.natDegree + 2 * C.natDegree + D.natDegree) =
      (45 / 2048 : k) * A.leadingCoeff * C.leadingCoeff ^ 2 * D.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * D.leadingCoeff * G.leadingCoeff + (-(15 / 256 : k)) * A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
  simp only [n3QuarticChamberFace9504810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * C ^ 2 * D)).natDegree = A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne (pow_ne_zero 2 hCne)) hDne, natDegree_mul hAne (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A * C ^ 2 * D)).coeff (A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff * C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * D * G)).natDegree = A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hGne, natDegree_mul hAne hDne]
    all_goals omega
  have hc1 : ((A * D * G)).coeff (A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A * E * F)).natDegree = A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hEne) hFne, natDegree_mul hAne hEne]
    all_goals omega
  have hc2 : ((A * E * F)).coeff (A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem n4QuarticChamberFace9505810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (hGne : G ≠ 0)
    (ht1 : 2 * A.natDegree + D.natDegree + G.natDegree = 2 * A.natDegree + 2 * C.natDegree + D.natDegree)
    (ht2 : 2 * A.natDegree + E.natDegree + F.natDegree = 2 * A.natDegree + 2 * C.natDegree + D.natDegree) :
    (n4QuarticChamberFace9505810 A B C D E F G).coeff
        (2 * A.natDegree + 2 * C.natDegree + D.natDegree) =
      (-(15 / 2048 : k)) * A.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 * D.leadingCoeff + (5 / 256 : k) * A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff + (5 / 256 : k) * A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
  simp only [n4QuarticChamberFace9505810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * C ^ 2 * D)).natDegree = 2 * A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) (pow_ne_zero 2 hCne)) hDne, natDegree_mul (pow_ne_zero 2 hAne) (pow_ne_zero 2 hCne)]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * C ^ 2 * D)).coeff (2 * A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff ^ 2 * C.leadingCoeff ^ 2 * D.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * D * G)).natDegree = 2 * A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hGne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * D * G)).coeff (2 * A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * G.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd2 : ((A ^ 2 * E * F)).natDegree = 2 * A.natDegree + 2 * C.natDegree + D.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hEne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hEne]
    simp only [natDegree_pow]
    all_goals omega
  have hc2 : ((A ^ 2 * E * F)).coeff (2 * A.natDegree + 2 * C.natDegree + D.natDegree) = A.leadingCoeff ^ 2 * E.leadingCoeff * F.leadingCoeff := by
    rw [← hd2, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1, hc2]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem nuQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (ht1 : A.natDegree + D.natDegree + E.natDegree = A.natDegree + C.natDegree + F.natDegree) :
    (nuQuarticChamberFace9502810 A B C D E F G).coeff
        (A.natDegree + C.natDegree + F.natDegree) =
      (-(5 / 128 : k)) * A.leadingCoeff * C.leadingCoeff * F.leadingCoeff + (-(5 / 128 : k)) * A.leadingCoeff * D.leadingCoeff * E.leadingCoeff := by
  simp only [nuQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A * C * F)).natDegree = A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hCne) hFne, natDegree_mul hAne hCne]
    all_goals omega
  have hc0 : ((A * C * F)).coeff (A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A * D * E)).natDegree = A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero hAne hDne) hEne, natDegree_mul hAne hDne]
    all_goals omega
  have hc1 : ((A * D * E)).coeff (A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem omicronQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (ht1 : 2 * A.natDegree + D.natDegree + E.natDegree = 2 * A.natDegree + C.natDegree + F.natDegree) :
    (omicronQuarticChamberFace9502810 A B C D E F G).coeff
        (2 * A.natDegree + C.natDegree + F.natDegree) =
      (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 2 * D.leadingCoeff * E.leadingCoeff := by
  simp only [omicronQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 2 * C * F)).natDegree = 2 * A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 2 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 2 * C * F)).coeff (2 * A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff ^ 2 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 2 * D * E)).natDegree = 2 * A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 2 hAne) hDne) hEne, natDegree_mul (pow_ne_zero 2 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 2 * D * E)).coeff (2 * A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff ^ 2 * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

set_option maxHeartbeats 64000000 in
theorem primitiveQuarticChamberFace9502810_coeff_top
    {A B C D E F G : k[X]}
    (hAne : A ≠ 0)
    (hCne : C ≠ 0)
    (hDne : D ≠ 0)
    (hEne : E ≠ 0)
    (hFne : F ≠ 0)
    (ht1 : 3 * A.natDegree + D.natDegree + E.natDegree = 3 * A.natDegree + C.natDegree + F.natDegree) :
    (primitiveQuarticChamberFace9502810 A B C D E F G).coeff
        (3 * A.natDegree + C.natDegree + F.natDegree) =
      (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff + (-(5 / 2048 : k)) * A.leadingCoeff ^ 3 * D.leadingCoeff * E.leadingCoeff := by
  simp only [primitiveQuarticChamberFace9502810, coeff_add, coeff_sub,
    coeff_smul, smul_eq_mul]
  have hd0 : ((A ^ 3 * C * F)).natDegree = 3 * A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hCne) hFne, natDegree_mul (pow_ne_zero 3 hAne) hCne]
    simp only [natDegree_pow]
    all_goals omega
  have hc0 : ((A ^ 3 * C * F)).coeff (3 * A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff ^ 3 * C.leadingCoeff * F.leadingCoeff := by
    rw [← hd0, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  have hd1 : ((A ^ 3 * D * E)).natDegree = 3 * A.natDegree + C.natDegree + F.natDegree := by
    rw [natDegree_mul (mul_ne_zero (pow_ne_zero 3 hAne) hDne) hEne, natDegree_mul (pow_ne_zero 3 hAne) hDne]
    simp only [natDegree_pow]
    all_goals omega
  have hc1 : ((A ^ 3 * D * E)).coeff (3 * A.natDegree + C.natDegree + F.natDegree) = A.leadingCoeff ^ 3 * D.leadingCoeff * E.leadingCoeff := by
    rw [← hd1, coeff_natDegree]
    all_goals simp only [leadingCoeff_mul, leadingCoeff_pow]
  rw [hc0, hc1]
  all_goals ring

/-! ## The scalar certificates -/

set_option maxHeartbeats 64000000 in
/-- 4 face rows force `g ^ 2 = 0`. -/
theorem quarticUnownedTree0810_cert
    (a b c d e f g : k)
    (gamma : k)
    (hr0 : (-(5 / 128 : k)) * a * d ^ 2 + (3 / 4 : k) * gamma * g = 0)
    (hr1 : (-(15 / 256 : k)) * a * d * g = 0)
    (hr2 : (5 / 256 : k) * a ^ 2 * d * g = 0)
    (hr3 : (5 / 4 : k) * g ^ 2 = 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : g ^ 2 = 0 := by
    linear_combination ((4 / 5 : k)) * hr3
  exact (pow_ne_zero 2 hGc) ht

set_option maxHeartbeats 64000000 in
/-- 3 face rows force `a * d * g = 0`. -/
theorem quarticUnownedTree1810_cert
    (a b c d e f g : k)
    (gamma : k)
    (hr0 : (-(5 / 128 : k)) * a * d ^ 2 + (3 / 4 : k) * gamma * g = 0)
    (hr1 : (-(15 / 256 : k)) * a * d * g = 0)
    (hr2 : (5 / 256 : k) * a ^ 2 * d * g = 0)
    (hAc : (a : k) ≠ 0)
    (hDc : (d : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : a * d * g = 0 := by
    linear_combination ((-256 / 15 : k)) * hr1
  exact (mul_ne_zero (mul_ne_zero hAc hDc) hGc) ht

set_option maxHeartbeats 64000000 in
/-- 7 face rows force `f ^ 2 * g ^ 2 = 0`. -/
theorem quarticUnownedTree2810_cert
    (a b c d e f g : k)
    (hr0 : (5 / 512 : k) * a * b ^ 3 + (-(15 / 128 : k)) * b * c ^ 2 + (5 / 16 : k) * b * g + (5 / 16 : k) * c * f = 0)
    (hr1 : (15 / 512 : k) * a * b ^ 2 * c + (-(5 / 64 : k)) * a * b * f + (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g = 0)
    (hr2 : (-(5 / 4096 : k)) * a ^ 2 * b ^ 3 + (15 / 1024 : k) * a * b * c ^ 2 + (-(5 / 128 : k)) * a * b * g + (-(5 / 128 : k)) * a * c * f = 0)
    (hr3 : (-(5 / 65536 : k)) * a ^ 3 * b ^ 3 + (15 / 16384 : k) * a ^ 2 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 2 * b * g + (-(5 / 2048 : k)) * a ^ 2 * c * f = 0)
    (hr4 : (15 / 4096 : k) * a ^ 2 * b ^ 4 + (-(45 / 512 : k)) * a * b ^ 2 * c ^ 2 + (5 / 64 : k) * a * b ^ 2 * g + (5 / 32 : k) * a * b * c * f + (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * a * f ^ 2 + (-(5 / 16 : k)) * c ^ 2 * g + (5 / 4 : k) * g ^ 2 = 0)
    (hr5 : (-(5 / 65536 : k)) * a ^ 4 * b ^ 3 + (15 / 16384 : k) * a ^ 3 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 3 * b * g + (-(5 / 2048 : k)) * a ^ 3 * c * f = 0)
    (hr6 : (-(15 / 2048 : k)) * a * b ^ 4 + (45 / 512 : k) * b ^ 2 * c ^ 2 + (-(5 / 64 : k)) * b ^ 2 * g + (-(25 / 128 : k)) * b * c * f + (5 / 32 : k) * f ^ 2 = 0)
    (hFc : (f : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : f ^ 2 * g ^ 2 = 0 := by
    linear_combination ((99 / 1120 : k) * a * b ^ 3 * c ^ 2 + (1011 / 14000 : k) * a * b ^ 3 * g + (-594 / 875 : k) * a * b ^ 2 * c * f + (3267 / 7000 : k) * a * b * f ^ 2 + (2761 / 3500 : k) * b * c ^ 2 * g + (1872 / 875 : k) * b * g ^ 2 + (-33 / 70 : k) * c * f * g) * hr0 + ((33 / 280 : k) * b ^ 2 * c ^ 3 + (-2411 / 1750 : k) * b ^ 2 * c * g + (-792 / 875 : k) * b * c ^ 2 * f + (187 / 70 : k) * b * f * g + (1089 / 1750 : k) * c * f ^ 2) * hr1 + ((11 / 140 : k) * b ^ 2 * c ^ 2 + (-599 / 1750 : k) * b ^ 2 * g + (-528 / 875 : k) * b * c * f + (363 / 875 : k) * f ^ 2) * hr4 + ((11 / 70 : k) * a * b ^ 2 * c ^ 2 + (-131 / 1750 : k) * a * b ^ 2 * g + (-1056 / 875 : k) * a * b * c * f + (726 / 875 : k) * a * f ^ 2 + (66 / 125 : k) * c ^ 2 * g + (2696 / 875 : k) * g ^ 2) * hr6
  exact (mul_ne_zero (pow_ne_zero 2 hFc) (pow_ne_zero 2 hGc)) ht

set_option maxHeartbeats 64000000 in
/-- 13 face rows force `f ^ 3 = 0`. -/
theorem quarticUnownedTree3810_cert
    (a b c d e f g : k)
    (hr0 : (5 / 512 : k) * a * b ^ 3 + (-(15 / 128 : k)) * b * c ^ 2 + (5 / 16 : k) * b * g + (5 / 16 : k) * c * f = 0)
    (hr1 : (15 / 512 : k) * a * b ^ 2 * c + (-(5 / 64 : k)) * a * b * f + (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g = 0)
    (hr2 : (35 / 512 : k) * b ^ 3 * c + (-(15 / 128 : k)) * b ^ 2 * f = 0)
    (hr3 : (-(175 / 8192 : k)) * a * b ^ 3 * c + (65 / 2048 : k) * a * b ^ 2 * f + (45 / 1024 : k) * b * c ^ 3 + (-(15 / 128 : k)) * b * c * g + (-(5 / 64 : k)) * c ^ 2 * f + (5 / 16 : k) * f * g = 0)
    (hr4 : (155 / 8192 : k) * a ^ 2 * b ^ 3 * c + (-(55 / 2048 : k)) * a ^ 2 * b ^ 2 * f + (-(15 / 256 : k)) * a * b * c ^ 3 + (5 / 32 : k) * a * b * c * g + (5 / 64 : k) * a * c ^ 2 * f + (-(5 / 8 : k)) * a * f * g = 0)
    (hr5 : (-(35 / 4096 : k)) * a * b ^ 3 * c + (5 / 512 : k) * a * b ^ 2 * f + (45 / 1024 : k) * b * c ^ 3 + (-(15 / 128 : k)) * b * c * g + (-(5 / 64 : k)) * c ^ 2 * f + (5 / 16 : k) * f * g = 0)
    (hr6 : (145 / 12288 : k) * a ^ 2 * b ^ 3 * c + (-(25 / 1536 : k)) * a ^ 2 * b ^ 2 * f + (-(45 / 1024 : k)) * a * b * c ^ 3 + (15 / 128 : k) * a * b * c * g + (5 / 96 : k) * a * c ^ 2 * f + (-(25 / 48 : k)) * a * f * g = 0)
    (hr7 : (-(5 / 2048 : k)) * a ^ 2 * b ^ 3 * c + (15 / 512 : k) * a * b * c ^ 3 + (-(5 / 64 : k)) * a * b * c * g + (-(5 / 64 : k)) * a * c ^ 2 * f = 0)
    (hr8 : (-(5 / 4096 : k)) * a ^ 2 * b ^ 3 + (15 / 1024 : k) * a * b * c ^ 2 + (-(5 / 128 : k)) * a * b * g + (-(5 / 128 : k)) * a * c * f = 0)
    (hr9 : (-(5 / 65536 : k)) * a ^ 3 * b ^ 3 + (15 / 16384 : k) * a ^ 2 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 2 * b * g + (-(5 / 2048 : k)) * a ^ 2 * c * f = 0)
    (hr10 : (15 / 4096 : k) * a ^ 2 * b ^ 4 + (-(45 / 512 : k)) * a * b ^ 2 * c ^ 2 + (5 / 64 : k) * a * b ^ 2 * g + (5 / 32 : k) * a * b * c * f + (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * a * f ^ 2 + (-(5 / 16 : k)) * c ^ 2 * g + (5 / 4 : k) * g ^ 2 = 0)
    (hr11 : (-(5 / 65536 : k)) * a ^ 4 * b ^ 3 + (15 / 16384 : k) * a ^ 3 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 3 * b * g + (-(5 / 2048 : k)) * a ^ 3 * c * f = 0)
    (hr12 : (-(15 / 2048 : k)) * a * b ^ 4 + (45 / 512 : k) * b ^ 2 * c ^ 2 + (-(5 / 64 : k)) * b ^ 2 * g + (-(25 / 128 : k)) * b * c * f + (5 / 32 : k) * f ^ 2 = 0)
    (hFc : (f : k) ≠ 0) :
    False := by
  have ht : f ^ 3 = 0 := by
    linear_combination ((-59 / 60 : k) * b ^ 2 * c + (38 / 9 : k) * b * f) * hr0 + ((-1 / 4 : k) * b ^ 3) * hr1 + ((-2 / 45 : k) * a * b ^ 2 + (16 / 9 : k) * g) * hr2 + ((-88 / 45 : k) * b ^ 2) * hr5 + ((-4 / 9 : k) * b * c + (32 / 5 : k) * f) * hr12
  exact (pow_ne_zero 3 hFc) ht

set_option maxHeartbeats 64000000 in
/-- 7 face rows force `f ^ 2 * g ^ 2 = 0`. -/
theorem quarticUnownedTree4810_cert
    (a b c d e f g : k)
    (hr0 : (5 / 512 : k) * a * b ^ 3 + (-(15 / 128 : k)) * b * c ^ 2 + (5 / 16 : k) * b * g + (5 / 16 : k) * c * f = 0)
    (hr1 : (15 / 512 : k) * a * b ^ 2 * c + (-(5 / 64 : k)) * a * b * f + (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g = 0)
    (hr2 : (-(5 / 4096 : k)) * a ^ 2 * b ^ 3 + (15 / 1024 : k) * a * b * c ^ 2 + (-(5 / 128 : k)) * a * b * g + (-(5 / 128 : k)) * a * c * f = 0)
    (hr3 : (-(5 / 65536 : k)) * a ^ 3 * b ^ 3 + (15 / 16384 : k) * a ^ 2 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 2 * b * g + (-(5 / 2048 : k)) * a ^ 2 * c * f = 0)
    (hr4 : (15 / 4096 : k) * a ^ 2 * b ^ 4 + (-(45 / 512 : k)) * a * b ^ 2 * c ^ 2 + (5 / 64 : k) * a * b ^ 2 * g + (5 / 32 : k) * a * b * c * f + (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * a * f ^ 2 + (-(5 / 16 : k)) * c ^ 2 * g + (5 / 4 : k) * g ^ 2 = 0)
    (hr5 : (-(5 / 65536 : k)) * a ^ 4 * b ^ 3 + (15 / 16384 : k) * a ^ 3 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 3 * b * g + (-(5 / 2048 : k)) * a ^ 3 * c * f = 0)
    (hr6 : (-(15 / 2048 : k)) * a * b ^ 4 + (45 / 512 : k) * b ^ 2 * c ^ 2 + (-(5 / 64 : k)) * b ^ 2 * g + (-(25 / 128 : k)) * b * c * f + (5 / 32 : k) * f ^ 2 = 0)
    (hFc : (f : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : f ^ 2 * g ^ 2 = 0 := by
    linear_combination ((99 / 1120 : k) * a * b ^ 3 * c ^ 2 + (1011 / 14000 : k) * a * b ^ 3 * g + (-594 / 875 : k) * a * b ^ 2 * c * f + (3267 / 7000 : k) * a * b * f ^ 2 + (2761 / 3500 : k) * b * c ^ 2 * g + (1872 / 875 : k) * b * g ^ 2 + (-33 / 70 : k) * c * f * g) * hr0 + ((33 / 280 : k) * b ^ 2 * c ^ 3 + (-2411 / 1750 : k) * b ^ 2 * c * g + (-792 / 875 : k) * b * c ^ 2 * f + (187 / 70 : k) * b * f * g + (1089 / 1750 : k) * c * f ^ 2) * hr1 + ((11 / 140 : k) * b ^ 2 * c ^ 2 + (-599 / 1750 : k) * b ^ 2 * g + (-528 / 875 : k) * b * c * f + (363 / 875 : k) * f ^ 2) * hr4 + ((11 / 70 : k) * a * b ^ 2 * c ^ 2 + (-131 / 1750 : k) * a * b ^ 2 * g + (-1056 / 875 : k) * a * b * c * f + (726 / 875 : k) * a * f ^ 2 + (66 / 125 : k) * c ^ 2 * g + (2696 / 875 : k) * g ^ 2) * hr6
  exact (mul_ne_zero (pow_ne_zero 2 hFc) (pow_ne_zero 2 hGc)) ht

set_option maxHeartbeats 64000000 in
/-- 10 face rows force `g ^ 3 = 0`. -/
theorem quarticUnownedTree5810_cert
    (a b c d e f g : k)
    (epsilon : k)
    (hr0 : (5 / 512 : k) * a * b ^ 3 + (-(15 / 128 : k)) * b * c ^ 2 + (5 / 16 : k) * b * g + (5 / 16 : k) * c * f = 0)
    (hr1 : (15 / 512 : k) * a * b ^ 2 * c + (-(5 / 64 : k)) * a * b * f + (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g = 0)
    (hr2 : (-(175 / 8192 : k)) * a * b ^ 3 * c + (65 / 2048 : k) * a * b ^ 2 * f + (45 / 1024 : k) * b * c ^ 3 + (-(15 / 128 : k)) * b * c * g + (-(5 / 64 : k)) * c ^ 2 * f + (5 / 16 : k) * f * g + (3 / 64 : k) * epsilon * a * b * c + (-(3 / 32 : k)) * epsilon * a * f = 0)
    (hr3 : (155 / 8192 : k) * a ^ 2 * b ^ 3 * c + (-(55 / 2048 : k)) * a ^ 2 * b ^ 2 * f + (-(15 / 256 : k)) * a * b * c ^ 3 + (5 / 32 : k) * a * b * c * g + (5 / 64 : k) * a * c ^ 2 * f + (-(5 / 8 : k)) * a * f * g + (-(1 / 64 : k)) * epsilon * a ^ 2 * b * c + (1 / 32 : k) * epsilon * a ^ 2 * f = 0)
    (hr4 : (145 / 12288 : k) * a ^ 2 * b ^ 3 * c + (-(25 / 1536 : k)) * a ^ 2 * b ^ 2 * f + (-(45 / 1024 : k)) * a * b * c ^ 3 + (15 / 128 : k) * a * b * c * g + (5 / 96 : k) * a * c ^ 2 * f + (-(25 / 48 : k)) * a * f * g = 0)
    (hr5 : (-(5 / 4096 : k)) * a ^ 2 * b ^ 3 + (15 / 1024 : k) * a * b * c ^ 2 + (-(5 / 128 : k)) * a * b * g + (-(5 / 128 : k)) * a * c * f = 0)
    (hr6 : (-(5 / 65536 : k)) * a ^ 3 * b ^ 3 + (15 / 16384 : k) * a ^ 2 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 2 * b * g + (-(5 / 2048 : k)) * a ^ 2 * c * f = 0)
    (hr7 : (15 / 4096 : k) * a ^ 2 * b ^ 4 + (-(45 / 512 : k)) * a * b ^ 2 * c ^ 2 + (5 / 64 : k) * a * b ^ 2 * g + (5 / 32 : k) * a * b * c * f + (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * a * f ^ 2 + (-(5 / 16 : k)) * c ^ 2 * g + (5 / 4 : k) * g ^ 2 = 0)
    (hr8 : (-(5 / 65536 : k)) * a ^ 4 * b ^ 3 + (15 / 16384 : k) * a ^ 3 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 3 * b * g + (-(5 / 2048 : k)) * a ^ 3 * c * f = 0)
    (hr9 : (-(15 / 2048 : k)) * a * b ^ 4 + (45 / 512 : k) * b ^ 2 * c ^ 2 + (-(5 / 64 : k)) * b ^ 2 * g + (-(25 / 128 : k)) * b * c * f + (5 / 32 : k) * f ^ 2 + (1 / 32 : k) * epsilon * a * b ^ 2 + (-(1 / 8 : k)) * epsilon * c ^ 2 + (1 / 2 : k) * epsilon * g = 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : g ^ 3 = 0 := by
    linear_combination ((-21 / 640 : k) * a ^ 2 * b ^ 3 + (9 / 50 : k) * a ^ 2 * b * epsilon + (1 / 4 : k) * a * b * c ^ 2 + (1 / 20 : k) * a * b * g + (-3 / 8 : k) * a * c * f) * hr0 + ((9 / 32 : k) * a * b ^ 2 * c + (1 / 20 : k) * a * b * f + (-18 / 25 : k) * a * c * epsilon + (-3 / 40 : k) * c ^ 3 + (1 : k) * c * g) * hr1 + ((6 / 5 : k) * a * b * c + (-8 / 5 : k) * a * f) * hr2 + ((21 / 20 : k) * b * c + (-9 / 5 : k) * f) * hr4 + ((-11 / 80 : k) * a * b ^ 2 + (12 / 25 : k) * a * epsilon + (-1 / 20 : k) * c ^ 2 + (4 / 5 : k) * g) * hr7 + ((-9 / 80 : k) * a ^ 2 * b ^ 2 + (9 / 20 : k) * a * c ^ 2 + (-6 / 5 : k) * a * g) * hr9
  exact (pow_ne_zero 3 hGc) ht

set_option maxHeartbeats 64000000 in
/-- 4 face rows force `e * g ^ 2 = 0`. -/
theorem quarticUnownedTree6810_cert
    (a b c d e f g : k)
    (hr0 : (-(15 / 256 : k)) * a * d * g + (-(15 / 256 : k)) * a * e * f = 0)
    (hr1 : (5 / 256 : k) * a ^ 2 * d * g + (5 / 256 : k) * a ^ 2 * e * f = 0)
    (hr2 : (-(5 / 16 : k)) * a * f ^ 2 + (5 / 4 : k) * g ^ 2 = 0)
    (hr3 : (-(5 / 64 : k)) * a * d * f + (5 / 16 : k) * e * g = 0)
    (hEc : (e : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : e * g ^ 2 = 0 := by
    linear_combination ((-32 / 15 : k) * f) * hr0 + ((2 / 5 : k) * e) * hr2 + ((8 / 5 : k) * g) * hr3
  exact (mul_ne_zero hEc (pow_ne_zero 2 hGc)) ht

set_option maxHeartbeats 64000000 in
/-- 2 face rows force `a * f * g = 0`. -/
theorem quarticUnownedTree7810_cert
    (a b c d e f g : k)
    (gamma : k)
    (hr0 : (5 / 32 : k) * e ^ 2 + (3 / 4 : k) * gamma * g = 0)
    (hr1 : (-(25 / 48 : k)) * a * f * g = 0)
    (hAc : (a : k) ≠ 0)
    (hFc : (f : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : a * f * g = 0 := by
    linear_combination ((-48 / 25 : k)) * hr1
  exact (mul_ne_zero (mul_ne_zero hAc hFc) hGc) ht

set_option maxHeartbeats 64000000 in
/-- 10 face rows force `e * g ^ 2 = 0`. -/
theorem quarticUnownedTree8810_cert
    (a b c d e f g : k)
    (hr0 : (-(15 / 128 : k)) * b * c ^ 2 + (5 / 16 : k) * b * g + (5 / 16 : k) * c * f + (5 / 16 : k) * d * e = 0)
    (hr1 : (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g + (5 / 32 : k) * e ^ 2 = 0)
    (hr2 : (-(15 / 64 : k)) * b * c * e + (-(15 / 128 : k)) * c ^ 2 * d + (5 / 16 : k) * d * g + (5 / 16 : k) * e * f = 0)
    (hr3 : (45 / 1024 : k) * a * b * c * e + (45 / 2048 : k) * a * c ^ 2 * d + (-(15 / 256 : k)) * a * d * g + (-(15 / 256 : k)) * a * e * f = 0)
    (hr4 : (-(15 / 1024 : k)) * a ^ 2 * b * c * e + (-(15 / 2048 : k)) * a ^ 2 * c ^ 2 * d + (5 / 256 : k) * a ^ 2 * d * g + (5 / 256 : k) * a ^ 2 * e * f = 0)
    (hr5 : (15 / 1024 : k) * a * b * c ^ 2 + (-(5 / 128 : k)) * a * b * g + (-(5 / 128 : k)) * a * c * f + (-(5 / 128 : k)) * a * d * e = 0)
    (hr6 : (15 / 16384 : k) * a ^ 2 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 2 * b * g + (-(5 / 2048 : k)) * a ^ 2 * c * f + (-(5 / 2048 : k)) * a ^ 2 * d * e = 0)
    (hr7 : (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * c ^ 2 * g + (-(5 / 8 : k)) * c * e ^ 2 + (5 / 4 : k) * g ^ 2 = 0)
    (hr8 : (15 / 16384 : k) * a ^ 3 * b * c ^ 2 + (-(5 / 2048 : k)) * a ^ 3 * b * g + (-(5 / 2048 : k)) * a ^ 3 * c * f + (-(5 / 2048 : k)) * a ^ 3 * d * e = 0)
    (hr9 : (-(15 / 128 : k)) * c ^ 2 * e + (5 / 16 : k) * e * g = 0)
    (hEc : (e : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : e * g ^ 2 = 0 := by
    linear_combination ((72 / 55 : k) * c * e) * hr1 + ((18 / 55 : k) * e) * hr7 + ((-3 / 11 : k) * c ^ 2 + (104 / 55 : k) * g) * hr9
  exact (mul_ne_zero hEc (pow_ne_zero 2 hGc)) ht

set_option maxHeartbeats 64000000 in
/-- 10 face rows force `c * f * g = 0`. -/
theorem quarticUnownedTree9810_cert
    (a b c d e f g : k)
    (hr0 : (5 / 16 : k) * c * f + (5 / 16 : k) * d * e = 0)
    (hr1 : (-(5 / 128 : k)) * c ^ 3 + (5 / 16 : k) * c * g + (5 / 32 : k) * e ^ 2 = 0)
    (hr2 : (-(15 / 128 : k)) * c ^ 2 * d + (5 / 16 : k) * d * g + (5 / 16 : k) * e * f = 0)
    (hr3 : (45 / 2048 : k) * a * c ^ 2 * d + (-(15 / 256 : k)) * a * d * g + (-(15 / 256 : k)) * a * e * f = 0)
    (hr4 : (-(15 / 2048 : k)) * a ^ 2 * c ^ 2 * d + (5 / 256 : k) * a ^ 2 * d * g + (5 / 256 : k) * a ^ 2 * e * f = 0)
    (hr5 : (-(5 / 128 : k)) * a * c * f + (-(5 / 128 : k)) * a * d * e = 0)
    (hr6 : (-(5 / 2048 : k)) * a ^ 2 * c * f + (-(5 / 2048 : k)) * a ^ 2 * d * e = 0)
    (hr7 : (15 / 256 : k) * c ^ 4 + (-(5 / 16 : k)) * c ^ 2 * g + (-(5 / 8 : k)) * c * e ^ 2 + (5 / 4 : k) * g ^ 2 = 0)
    (hr8 : (-(5 / 2048 : k)) * a ^ 3 * c * f + (-(5 / 2048 : k)) * a ^ 3 * d * e = 0)
    (hr9 : (-(15 / 128 : k)) * c ^ 2 * e + (5 / 16 : k) * e * g = 0)
    (hCc : (c : k) ≠ 0)
    (hFc : (f : k) ≠ 0)
    (hGc : (g : k) ≠ 0) :
    False := by
  have ht : c * f * g = 0 := by
    linear_combination ((3 / 5 : k) * c ^ 2 + (-8 / 5 : k) * g) * hr0 + ((24 / 5 : k) * f) * hr1 + ((-12 / 5 : k) * e) * hr2 + ((4 : k) * d) * hr9
  exact (mul_ne_zero (mul_ne_zero hCc hFc) hGc) ht


end QuarticChamberUnownedTreeCerts810

end Max11DegreeRoutes

import Grok810ScaleZeroQuarticMultiKills2SpeedReflSplitPart3Scratch

/-! # SPEED: drop-in for `Grok810ScaleZeroQuarticMultiKills2Scratch`.  Every declaration now lives in the
`SplitBase` / `SplitPart*` modules this imports, so every name the original
module exported is still in scope. -/

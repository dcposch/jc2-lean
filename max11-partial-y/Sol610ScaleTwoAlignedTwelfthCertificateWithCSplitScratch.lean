import Sol610ScaleTwoAlignedTwelfthCertificateWithCSplitPart0Scratch

/-! # SPEED: drop-in for `Sol610ScaleTwoAlignedTwelfthCertificateWithCScratch`.  Every declaration now lives in the
`SplitBase` / `SplitPart*` modules this imports, so every name the original
module exported is still in scope. -/

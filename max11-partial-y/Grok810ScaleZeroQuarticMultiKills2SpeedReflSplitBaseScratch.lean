import Grok810ScaleZeroQuarticMultiKills2SpeedReflSplitBasePart105Scratch

/-! # Quartic Stage A multi-cone certificates (part 2: remaining 4-letter cones) -/

/-! This module is now a pure import aggregator: every declaration lives in the
chained `Part..` modules (already present in the tree, one declaration each), so
that no single module elaborates them all at once.  Statements, names, binders
and section contexts are byte-identical. -/

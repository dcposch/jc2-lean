import Grok810ScaleZeroQuarticMultiKills1bSpeedReflSplitPart3Scratch

/-! # SPEED: drop-in for `Grok810ScaleZeroQuarticMultiKills1bScratch`.  Every declaration now lives in the
`SplitBase` / `SplitPart*` modules this imports, so every name the original
module exported is still in scope. -/
